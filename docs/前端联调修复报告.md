# 前端联调修复报告

## 阶段 G（2026-05-03）— 文档收口与交付

- **修改 `src`**：**否**（本轮 **仅 `docs`**）。
- **产出**：**`docs/后端接口修复要求_交付版.md`**（交后端）、**`docs/成员3后台前端联调交付报告.md`**（组内汇报）；更新 **`docs/联调总进度.md`**（含 **curl** 冒烟表与遗留测试数据）、**`docs/后端待修复清单.md`**（摘要索引 **A～H**、**§8** 与 **E-6.2** 对齐）、**`docs/接口字段差异记录.md`**（写入 **camelCase** 汇总表）。
- **冒烟**：**curl** 直连 **`127.0.0.1:9876`** **10** 条 **GET**，均 **`success:true`**，见 **`docs/联调总进度.md`**。
- **type-check**：已执行 **`npm run type-check`**，**通过**。

---

## 模块与主要涉及文件索引（累计，阶段 G 收口）

**原则**：数据库字段语义仍在 **`src/types`**；**HTTP 适配在 `src/api`**；**UI 在 `src/views`**；编辑器 **`src/components/editor`** + **`src/stores/editor.ts`**；工具 **`src/utils/page-node-save.ts`**、**`response-utils`**。

| 模块 | 主要文件（示例） |
|------|------------------|
| **page** | `src/api/page.ts`，`src/views/page/PageListView.vue`，`PageFormView.vue` |
| **menu** | `src/api/menu.ts`，`src/views/menu/MenuManagementView.vue` |
| **component-def** | `src/api/component-def.ts`，`src/views/component-def/ComponentDefListView.vue`，`ComponentDefFormView.vue` |
| **data-binding** | `src/api/data-binding.ts`，`src/views/data-binding/DataBindingListView.vue`，`DataBindingFormView.vue` |
| **page-template** | `src/api/page-template.ts`，`src/views/page-template/PageTemplateListView.vue`，`PageTemplateFormView.vue` |
| **reusable-fragment** | `src/api/reusable-fragment.ts`，`src/views/reusable-fragment/FragmentListView.vue`，`FragmentFormView.vue` |
| **page-version** | `src/api/page-version.ts`，`src/views/page-version/PageVersionListView.vue` |
| **page-node / node-tree** | `src/api/page-node.ts`，`src/api/response-utils.ts` |
| **编辑器** | `src/stores/editor.ts`，`src/components/editor/EditorWorkspace.vue`，`src/views/editor/PageEditorView.vue`，`TemplateEditorView.vue`，`FragmentEditorView.vue` |

**改动类型归类**：API 真实接入与 camelCase 映射；列表 **`records`/`items`** 兼容；UI **`try/catch` + ElMessage**；属性面板 **JSON 校验**；整树保存 **`buildSaveNodeTreeRequestBodyForBackend`**；保存成功 **`loadEditor`**；**文档修正**。  
**数据库 / 接口契约**：均未擅自修改。  
**type-check**：**通过**（见阶段 G 小节）。

---

## 阶段 E-7（2026-05-03）

- **修改 `src`**：**是** — **`src/components/editor/EditorWorkspace.vue`**
- **原因**：保存成功后仅 **`page_version`** 调用 **`loadEditor`**，模板 / 片段未从服务端重拉，与页眉「保存后自动重载」不一致，且不利于 **`id` 与后端字段** 对齐。
- **修复**：**`handleSave`** 成功分支对 **`page_template` / `reusable_fragment`** 与 **`page_version`** 同样 **`await loadEditor(...)`**，失败 **`ElMessage.warning`**。
- **静态核对（未跑 dev）**：
  - 路由：**`/page-templates/:id/editor`**、**`/reusable-fragments/:id/editor`**；列表 **「编辑器」** 跳转。
  - **`loadEditor`**：**模板** **`getTemplateNodeTreeNodes`**；**片段** **`getFragmentNodeTreeNodes`**。
  - **`saveTree`**：**`saveTemplateNodeTree` / `saveFragmentNodeTree`**（ **`buildSaveNodeTreeRequestBodyForBackend`** ）。
  - **模板/片段**：新增 / 改属性 / 删节点均为本地 + **`validateNodes`** + 整树 **`saveTree`**；**`patchNode`** / **`moveNode`** / **`deleteNode`** 在无版本上下文时为本地逻辑。
  - **JSON**：**`handlePatch`** 已 **`validatePatchJsonFields`**（全 **`sourceType`** 共用）。
  - **保存失败**：API **`throw`** → **`handleSave` `catch`** → **`ElMessage.error`**；**`!response`** → **`ElMessage.error`**，不假成功。
- **人工 UI / Network**：请在本地 **`npm run dev`** 后新建专用测试模板/片段（**勿动 id 1～3**），确认 **`GET/PUT /api/page-templates|reusable-fragments/{id}/node-tree`** 请求体 **`{ nodes }`** camelCase，保存后再入编辑器节点仍存在；测毕删除测试数据。
- **type-check**：**通过**

---

## 阶段 E-6.2（2026-05-03）

- **修改 `src`**：**否**。
- **内容**：**curl** 直连 **`127.0.0.1:9876`**（**无 `/api`**）：**POST** 专用模板/片段 → **GET node-tree**（初始 **`nodes: []`**）→ **camelCase `PUT …/node-tree`**（**`{ nodes }`** 单根）→ **GET** 校验 → **DELETE** 清理；详见 **`docs/联调总进度.md`**。
- **结论**：模板 / 片段整树保存路径在本机 **可按 camelCase 平铺 nodes 正确持久化**，未发现 **`parentId` 全 null** 类毁树（与 **E-5** snake_case 版本路径对比）。
- **type-check**：已执行 **`npm run type-check`**，**通过**（本轮无 **`src`** 变更，仅为惯例确认）。

---

## 阶段 E-6.1（2026-05-03）

- **修改 `src`**：**否**（仅 curl 复核与 **`docs`** 修正）。
- **内容**：复核 **`127.0.0.1:9876`** 上 **`GET /page-templates`** 与 **`GET /api/page-templates`**（及 **`reusable-fragments`** 对称）；确认直连业务路径 **无 `/api`**；只读 **`GET …/node-tree`**（**`templateId=1`**、**`fragmentId=1`**）；**未做 PUT**。
- **结论**：**E-6** 阶段 **`GET …/api/page-templates`** 的 **`SYSTEM_ERROR`** 为 **curl 路径误用**，非模板/片段列表接口不可用；详见 **`docs/联调总进度.md`**、**`docs/后端待修复清单.md`** 路径约定。

---

## 本轮（2026-05-03）

### E-6（阶段）：模板 / 片段 node-tree 整树 PUT camelCase

- **修改文件**：
  - `src/api/page-node.ts`
  - `src/api/page-template.ts`
  - `src/api/reusable-fragment.ts`
- **修复内容**：
  - 将 **`buildSaveVersionNodeTreeRequestBodyCamel`** 重命名并导出为 **`buildSaveNodeTreeRequestBodyForBackend`**（仍为 **`{ nodes: [...] }`** 平铺，**JSON 字段不 parse**，**`null` 保留**）。
  - **`saveVersionNodeTree`**、**`saveTemplateNodeTree`**、**`saveFragmentNodeTree`** 真实分支均 **`JSON.stringify(buildSaveNodeTreeRequestBodyForBackend(payload))`**；store / **`PageNodeTreeWriteItem`** 仍为 snake_case。
  - **`success:false`** 等行为不变（沿用既有信封校验与 **`throw`**，不假成功）。
- **curl（已澄清）**：**E-6** 曾误用 **`/api`**；**E-6.1** 纠正直连路径。**模板/片段整树 PUT** 的 **curl 闭环** 已由 **E-6.2** 完成（专用新建资源后删除）。
- **是否影响数据库**：否
- **是否影响接口契约**：否
- **type-check 结果**：通过

---

### reusable-fragment

- **修改文件**：
  - `src/views/reusable-fragment/FragmentListView.vue`
  - `src/views/reusable-fragment/FragmentFormView.vue`
- **修复内容**：
  - 列表 `load`：`try/catch` + `ElMessage.error`，失败清空列表避免展示脏数据
  - 删除：`confirmDelete` 内 `try/catch`，成功/失败均提示并 `load`，不假删
  - 表单：`load`/`submit` 增加错误提示；提交 payload 显式字段与 trim，不包含多余展开字段；新建不传 `id`
- **是否影响数据库**：否
- **是否影响接口契约**：否（仍按既有 API 层与类型）
- **type-check 结果**：通过

### page-version

- **修改文件**：`src/views/page-version/PageVersionListView.vue`
- **修复内容**：
  - 列表 `load`、`handleCreate` 已有错误处理基础上，为 **发布 / 复制 / 锁定切换 / 删除** 增加独立 handler：`try/catch` + `ElMessage`，失败后 `load` 刷新列表状态
- **是否影响数据库**：否
- **是否影响接口契约**：否
- **type-check 结果**：通过

---

## 历史摘要（上下文承接）

- **page-template**：列表/表单错误处理、API 真实分支与列表 `records`/`items`、写入 camelCase 适配（见 `src/api/page-template.ts`）。
- **page-version API**：`listPageVersions` 等真实分支；详情接口遇后端 SYSTEM_ERROR 时抛错（不修契约）。
- **reusable-fragment API**：与模板对称的 CRUD + node-tree 信封校验。

---

## 阶段 E-1（2026-05-03）

- page-node / node-tree 扫描与三类 GET 只读探测；**未修改 `src`**（详见当时 `docs/` 版本）。

## 阶段 E-3（2026-05-03）

- **`src/stores/editor.ts`**
  - 引入 **`createPageNode`、`deletePageNode`**；新增 **`pickContentUpdatePayload`**、**`createEditorNode`**。
  - **页面版本**：**`deleteNode`** → **`deletePageNode`** + **`loadEditor`**；**`createEditorNode`** → **`createPageNode`** + **`loadEditor`**；**`patchNode`** 仅提交内容字段；**`moveNode` / `copyNode`** 成功后以 **`loadEditor`** 结果为准解析返回节点。
  - **模板 / 片段**：删除等行为仍为本地逻辑（与 E-2 一致）。
- **`src/components/editor/EditorWorkspace.vue`**（最小）
  - 新增/移动/删除/复制/属性/片段对话框路径增加 **`try/catch`** + **`ElMessage`**，承接 API **throw**。
  - 页面版本删除成功文案区分服务端删除。
- **type-check**：通过

## 阶段 E-4（2026-05-03）

- **`src/components/editor/EditorWorkspace.vue`**
  - **`load`**：`try/catch` + **`ElMessage.error`**（编辑器数据加载失败可见）。
  - **`handlePatch`**：`props_json` / `style_json` / `layout_json` / `event_json` / `visible_rule_json` 非空时 **`JSON.parse`** 校验后再 **`patchNode`**。
  - **`handleSave`**：`catch` 展示 **`Error.message`**；**页面版本**保存成功后 **`loadEditor`** 再对齐服务端树（再加载失败 **`ElMessage.warning`**）。
- **备注**：未在本环境启动 dev；**curl** 已对 **versionId=307** 做单节点链路与清理。
- **type-check**：通过

## 阶段 E-5（2026-05-03）

- **`src/api/page-node.ts`**
  - **`saveVersionNodeTree`**（真实模式）：请求体使用 **camelCase 构造函数**（**E-6** 起统一为 **`buildSaveNodeTreeRequestBodyForBackend`**），避免 **`JSON.stringify(SavePageNodeTreePayload)`**（snake_case）导致后端忽略父子绑定、树扁平化（curl：`307`，snake_case 模拟）。
- **curl**：**camelCase** 原样 **`nodes`** PUT **成功**，结构与 **`propsJson`** 等字符串字段保持；**snake_case** PUT **破坏父子链**，随后已用 **camelCase** 备份恢复 **307** 树。
- **后端**：记录 **`id_map` 换 id**（§9）、snake_case 绑定（§8）。
- **type-check**：通过

---

## 阶段 E-2（2026-05-03）

- **`src/api/page-node.ts`**
  - **`fetchRealVersionNodeTree`**：`success === false` 时抛出后端文案，避免误判为空树。
  - **真实模式**：`createPageNode`、`updatePageNode`、`movePageNode`、`deletePageNode` 对接 HTTP；camelCase 请求体由适配函数生成；`success:false` **throw**，不使用 mock 兜底。
  - **`copyPageNode`**：响应信封增加 `success:false` 校验。
  - **辅助**：`throwIfEnvelopeFailed`、`registerVersionNodesInOwnerCache`、`extractPageNodeFromEnvelopeResponse`、`extractDeletePageNodeResponseId`。
- **是否影响数据库**：否
- **是否影响接口契约**：否（文档未改，仅实现侧适配）
- **type-check**：通过

---
