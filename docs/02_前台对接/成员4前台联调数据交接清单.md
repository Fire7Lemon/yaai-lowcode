# 成员4前台联调数据交接清单

> **生成时间**：2026-05-25（第八轮全链路 GET 冒烟）  
> **数据来源**：`http://127.0.0.1:9876` 联调库，**仅 GET 验证**，未写库、未改代码。  
> **关联文档**：[前台接入后台低代码说明.md](./前台接入后台低代码说明.md)、[component_key白名单.md](../01_设计基线/component_key白名单.md)、[数据修正执行记录.md](../05_数据库清洗/数据修正执行记录.md)

---

## 1. 当前联调状态

- 后台低代码 **页面域数据已重建**：5 个 `page`、5 个 `menu`、5 个 `page_version`（308～312）、对应 `node-tree` 均已就绪。
- **`menus` → `pages` → `node-tree`** 链路 **GET 全部成功**（第八轮冒烟通过）。
- 所有 **`page_version.status` 仍为 `draft`**，这是 **联调临时方案**；**不要**以 `published` 作为前台可否展示的唯一条件。
- **`page.currentVersionId`** 已通过 **`PUT /pages/{id}`** 临时绑定（第五轮）；契约 §5.4 未声明该可写字段，正式上线前应改走 **publish** 链路。
- **`POST /page-versions/{id}/publish`** 仍 **`SYSTEM_ERROR`**（第四轮起未修复），**正式发布前必须修复**。

---

## 2. 前台消费链路

推荐实现顺序（与 [前台接入后台低代码说明.md](./前台接入后台低代码说明.md) §3 一致）：

1. **`GET /menus`**（或封装为 `public-menu.ts`）  
   - 组装导航；`urlType=page` 时取 `pageId`。
2. **`GET /pages`**（或封装为 `public-page.ts`）  
   - 建立 `pageId → page` 与 `path → page` 索引。
3. 用户进入路由 → 用 **`route.path`** 匹配 **`page.path`**（注意 `/about/introduction` 等精确匹配）。
4. 读取匹配到的 **`page.currentVersionId`**。
5. **`GET /page-versions/{currentVersionId}/node-tree`**（或封装为 `public-node-tree.ts`）  
   - 响应为 **camelCase**；`data.nodes` 为 **平铺数组**。
6. **flat nodes → tree**：按 **`parentId`**（`null` 为根）、**`slotName`**、**`sortOrder`** 组装。
7. **`NodeRenderer`** 递归渲染；按 **`componentKey`** 查 **`component-map.ts`**。
8. **`propsJson` / `styleJson` / `layoutJson`** 在运行时 **`JSON.parse`**，解析失败需兜底为空对象，避免白屏。
9. 未知 **`componentKey`** → **`UnknownComponent`** 兜底，**不得**抛错导致整页崩溃。

**接口基址**：

| 场景 | 基址 |
|------|------|
| curl 直连后端 | `http://127.0.0.1:9876/...`（**不加** `/api`） |
| YAAI / Vite 开发 | 经代理 **`/api/...`**（以项目 `VITE_API_BASE_URL` 为准） |

---

## 3. 当前可用页面

| 路径 | pageId | currentVersionId | node-tree 状态 |
|------|-------:|-----------------:|----------------|
| `/` | 7 | 308 | 首页完整 **9** 节点 |
| `/news` | 8 | 309 | `page_container` + `rich_text`（**2** 节点） |
| `/services` | 9 | 310 | `page_container` + `rich_text`（**2** 节点） |
| `/conference` | 10 | 311 | `page_container` + `rich_text`（**2** 节点） |
| `/about/introduction` | 11 | 312 | `page_container` + `rich_text`（**2** 节点） |

**pages 明细**（第八轮 GET）：

| id | name | code | path | currentVersionId | status |
|----|------|------|------|----------------:|--------|
| 7 | 首页 | `home` | `/` | 308 | `true` |
| 8 | 新闻动态 | `news` | `/news` | 309 | `true` |
| 9 | 服务矩阵 | `services` | `/services` | 310 | `true` |
| 10 | 学术会议 | `conference` | `/conference` | 311 | `true` |
| 11 | 学会介绍 | `about_introduction` | `/about/introduction` | 312 | `true` |

---

## 4. 当前菜单数据

| menuId | name | code | pageId | 对应 path | sortOrder |
|-------:|------|------|-------:|-----------|----------:|
| 9 | 首页 | `nav_home` | 7 | `/` | 10 |
| 10 | 学会介绍 | `nav_about` | 11 | `/about/introduction` | 20 |
| 11 | 新闻动态 | `nav_news` | 8 | `/news` | 30 |
| 12 | 服务矩阵 | `nav_services` | 9 | `/services` | 40 |
| 13 | 学术会议 | `nav_conference` | 10 | `/conference` | 50 |

**校验摘要**：`total=5`；全部 **`urlType=page`**；全部 **`externalUrl=null`**；`pageId` 均指向有效 `page`；`sortOrder` 10→50 递增。

---

## 5. 首页 node-tree（version 308）

**结构**（单根 **4124**，8 个子节点，**`parentId` 均指向 4124**）：

```text
page_container (id=4124)
  hero_banner (4125)
  notice_list (4126)
  quick_links (4127)
  home_services (4128)
  home_events (4129)
  news_list (4130)
  home_shortcuts (4131)
  friend_links (4132)
```

**version 308 `status=draft`**（联调态，见 §8 风险）。

| nodeId | componentKey | nodeName | propsJson 主要字段 | dataBindingId |
|-------:|--------------|----------|-------------------|--------------:|
| 4124 | `page_container` | 首页根容器 | `title: "首页"` | — |
| 4125 | `hero_banner` | 首页轮播 | `title`, `subtitle`, `autoplay` | **2** |
| 4126 | `notice_list` | 通知公告 | `title`, `limit`, `showDate` | **3** |
| 4127 | `quick_links` | 快捷入口 | `title`, `items: []` | — |
| 4128 | `home_services` | 服务矩阵 | `title`, `subtitle`, `columns`, `items: []` | — |
| 4129 | `home_events` | 活动预告 | `title`, `limit`, `showDate` | — |
| 4130 | `news_list` | 新闻动态 | `title`, `limit`, `showDate` | **1** |
| 4131 | `home_shortcuts` | 快捷导航 | `title`, `items: []` | — |
| 4132 | `friend_links` | 友情链接 | `title`, `items: []` | — |

**`dataBindingId` 与 `data_bindings` 对应**（第八轮 GET 验证有效）：

| dataBindingId | 绑定名称 | bindingType | sourceKey | 引用节点 |
|--------------:|----------|-------------|-----------|----------|
| 1 | 学会新闻列表 | `article_category` | `news_category_xhxw` | **4130** `news_list` |
| 2 | 首页轮播图 | `banner_group` | `home_banner` | **4125** `hero_banner` |
| 3 | 通知公告列表 | `article_category` | `notice_category` | **4126** `notice_list` |

**YAAI 现有静态组件对照**（只读扫描 `../YAAI/src/views/home/sections/`）：

| componentKey | 建议映射 YAAI 组件 |
|--------------|-------------------|
| `hero_banner` | `HomeCarousel.vue` |
| `notice_list` | `HomeNotice.vue` |
| `quick_links` | `HomeQuickLinks.vue` |
| `home_services` | `HomeServices.vue` |
| `home_events` | `HomeEvents.vue` |
| `news_list` | `HomeNews.vue` |
| `home_shortcuts` | `HomeShortcuts.vue` |
| `friend_links` | `HomeFriendLinks.vue` |

---

## 6. 其它页面最小 node-tree

> **2026-05-30 更新（第十一轮，componentKey 语义标准化）**：子页面 key 已与首页区块 key 拆开。`/news`→`news_list`（新闻列表页）、`/services`→`services_page`、`/conference`→`conference_page`、`/about/introduction` 保持 `rich_text` 正式文案。首页 308 内的新闻区块已由 `news_list` 改为 `home_news`（与子页面 `news_list` 区分），其 home_services/home_events 首页区块不变。结构均保持单根 `page_container` + 单子（2 节点），`parentId` 不变。

| version | path | rootId | 子节点 id | 子节点 componentKey | dataBindingId | 说明 |
|--------:|------|-------:|----------:|---------------------|:-------------:|------|
| 309 | `/news` | 4133 | **4134** | **`news_list`**（新闻列表页） | **1**（`news_category_xhxw`） | 成员4 待补列表页组件，可复用 `HomeNews` 风格 |
| 310 | `/services` | 4135 | **4136** | **`services_page`** | null | `propsJson`：`title/subtitle/columns:4`；成员4 待补服务矩阵页组件 |
| 311 | `/conference` | 4137 | **4138** | **`conference_page`** | null | `propsJson`：`title/limit:8/showDate:true`；成员4 待补学术会议页组件 |
| 312 | `/about/introduction` | 4139 | **4140** | `rich_text` | null | `propsJson`：`title`+`content`（正式学会介绍文案） |

> **首页 308 内**：新闻区块节点 **4130** 已由 `news_list` → **`home_news`**（首页 key）；`home_services`(4128)/`home_events`(4129) 仍为首页区块，未变。
> **成员4 待新增 `component-map` 映射**：`home_news`→`HomeNews`、`services_page`→服务矩阵页组件、`conference_page`→学术会议页组件。补齐前这三类节点走 `UnknownComponent` 兜底（不白屏）。

- `news_list`(309) 通过 `dataBindingId=1` 关联 `news_category_xhxw`，与 YAAI `fetchNewsListByCategoryId` 语义对齐；成员4 组件层无需直接拉 data-binding 接口。
- `home_services`/`home_events`/`news_list` 当前**不依赖新后端业务接口**；YAAI 对应 `HomeServices`/`HomeEvents`/`HomeNews` 组件内置/自取数据，`propsJson`（含 `items`）仅作可选覆盖（`items` 实际透传为后续 P2，不阻塞展示）。
- `312` 子节点 **`rich_text.propsJson`** 含 `title` + `content` 字符串，可直接渲染静态段落。
- 其余字段（`parentId`/`slotName=main`/`sortOrder`）均未变；首页 308 仍 9 节点，仅其中新闻节点 4130 由 `news_list` 改为 `home_news`（其余 8 节点不变）。

---

## 7. component_key 白名单

**正式白名单（13 个，均已存在于 `component_def` 且 `status=true`）**：

- `page_container`
- `hero_banner`
- `notice_list`
- `quick_links`
- `home_services`（首页服务区块）
- `home_events`（首页活动区块）
- `home_news`（首页新闻区块，2026-05-30 新增；id=17）
- `news_list`（新闻列表页）
- `services_page`（服务矩阵页，2026-05-30 新增；id=18）
- `conference_page`（学术会议页，2026-05-30 新增；id=19）
- `home_shortcuts`
- `friend_links`
- `rich_text`

**历史别名（禁止使用）**：

- `service_matrix` → 统一为 **`home_services`**
- `event_list` → 统一为 **`home_events`**

**库中非白名单但保留的 `component_def`**（第八轮 GET，**勿在新节点中使用**）：

| componentKey | status | 说明 |
|------------|--------|------|
| `container` | `true` | 通用容器，历史定义 |
| `hero_split_container` | `true` | 首屏分栏容器 |
| `grid_container` | `true` | 栅格容器 |
| `nav_menu` | `true` | 导航菜单 |
| `copyright` | `true` | 版权信息 |
| `test_component_1777798334444` | **`false`** | 联调测试组件，已禁用 |

**`component-map.ts` 约定**：仅映射上表 10 个白名单键；未知键 → **`UnknownComponent`**。

---

## 8. 当前已知风险

1. **`page_version` 仍为 `draft`**：308～312 的 **`page_version.status`** 均为 **`draft`**，非正式发布态。
2. **`publish` 接口仍 `SYSTEM_ERROR`**：`POST /page-versions/{id}/publish` 不可用（见 [后端待修复清单.md](../04_后端修复/后端待修复清单.md) §10）。
3. **`currentVersionId` 是联调临时绑定**：通过 **`PUT /pages/{id}`** 写入，非 publish 自动维护；契约与 `src/api/page.ts` 适配层均未声明该写字段。
4. **联调阶段展示判断**：成员 4 **不要**依赖 **`version.status === 'published'`** 决定是否渲染；**以 `page.currentVersionId` 可拉到 node-tree 为准**。
5. **正式上线前必须修复 publish**：否则无法走标准发布闭环，`currentVersionId` 临时方案不可用于生产。

---

## 9. 成员4第一阶段开发建议

1. **先实现 API 适配层**（建议独立文件，勿与后台 `yaai-lowcode/src/api` 混用）：
   - `public-menu.ts` → `GET /menus`
   - `public-page.ts` → `GET /pages`
   - `public-node-tree.ts` → `GET /page-versions/{versionId}/node-tree`
2. **实现 flat nodes → tree** 工具函数（按 `parentId` / `sortOrder` / `slotName`）。
3. **实现 `component-map.ts`**，白名单 10 键映射到 Vue 组件；未知键走 **`UnknownComponent`**。
4. **首页优先接 version 308**：复用 YAAI 现有 `views/home/sections/*` 组件，按 `componentKey` 动态挂载。
5. **其它四页先用 `rich_text` 兜底**：保证路由切换时能渲染占位内容。
6. **再逐步替换**：将 `/news`、`/services` 等页的 `rich_text` 换为真实业务 section，与 YAAI 现有 `views/*` 对齐。

**YAAI 路由对齐提示**（只读扫描 `../YAAI/src/router/index.ts`）：

- 后台 **`page.path`** 与 YAAI 路由一致：`/`、`/news`、`/services`、`/conference`、`/about/introduction`。
- `/news` 在 YAAI 中为带侧栏的子路由体系；动态化时可先匹配 **`path=/news`** 的 page，再决定是替换整页还是仅替换主内容区。
- `/about/introduction` 对应 `views/about/Introduction.vue`；`rich_text` 可映射 **`AboutStaticArticle.vue`**。

---

## 10. 前后台适配分工（接 [前后台适配分工决策表.md](./前后台适配分工决策表.md)）

### 10.1 成员 3 已适配成员 4 现有结构的部分

- **路由路径**：5 个 `page.path`（`/`、`/news`、`/services`、`/conference`、`/about/introduction`）均**贴合 YAAI 现有路由 / 动态注册逻辑**，未要求成员 4 重命名或调整。
- **`code='home'` 内置识别**：`page 7` 的 `code='home'`，与 YAAI `BUILTIN_MODULE_CODES = new Set(['home'])` **精确对齐**，无需成员 4 调整内置模块识别。
- **`SitePage.currentVersionId` 类型预留**：成员 3 已用 `PUT /pages` 临时为 5 页绑定 `currentVersionId`，与 YAAI `types/sitePage.ts` 已存在的 `currentVersionId: number | null` 字段对齐。
- **菜单结构**：5 条菜单 `urlType='page'` + `pageId` 全部有效，**直接被 `siteMenusStore.navItems` 消费**渲染顶栏，无需成员 4 改 store。
- **响应 envelope**：保持 `{code, success, message, data}` + camelCase，与 `utils/request.ts` 拦截器 100% 兼容。
- **首页 308 节点 `componentKey`**：8 个白名单业务键与 `views/home/sections/*` 8 个组件**一一可映射**，成员 4 仅需在 `component-map.ts` 中静态映射。
- **`dataBindingId` 与 sourceKey**：1/2/3 对应 `news_category_xhxw` / `home_banner` / `notice_category`，与 YAAI 现有 `fetchNewsListByCategoryId` / `fetchHomeBanners` / `fetchLatestNews` 三个 API 的 `sourceKey` 语义对齐，**成员 4 在组件层无需直接拉 `data-binding` 接口**。

### 10.2 成员 4 仍需向低代码适配的部分

> 详见 [成员4向低代码适配实现指南.md](./成员4向低代码适配实现指南.md)。

| 优先级 | 任务 |
|:------:|------|
| **P0** | 修 `src/utils/sitePageNav.ts.shouldRegisterDynamicRoute`：放开 `pageType='portal'` 且非静态路由集合的 path，让 `/news`、`/about/introduction` 落到 `DynamicPage` |
| **P1** | 新增 `src/api/public-node-tree.ts`、`src/types/lowcode.ts`、`src/utils/buildNodeTree.ts`、`src/components/lowcode/{NodeRenderer,component-map,UnknownComponent,RichTextBlock}.vue` |
| **P2** | 改写 `src/views/cms/DynamicPage.vue`：完成 fetch → build → render 闭环；（可选）首页迁移到 DynamicPage 渲染 308 |

### 10.3 不建议成员 3 继续改数据绕过前台缺失运行时

- **不**因 YAAI 暂缺 `NodeRenderer` 而改 `page.pageType` 来"避开" portal 路由阻塞 — 让成员 4 改 `shouldRegisterDynamicRoute`，**不动后台数据语义**。
- **不**为了"让某个组件能展示"而临时改 `componentKey` 命名（白名单冻结）。
- **不**因前台暂缺富文本组件而把 `rich_text.propsJson.content` 改为非字符串结构（保持 `string`）。
- **不**因前台 component-map 暂未实现而引入历史别名 `service_matrix` / `event_list`。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-25 | 首版：第八轮全链路 GET 冒烟通过后生成，供成员 4 联调接入 |
| 2026-05-25 | 增补 §10：成员 3 已适配 / 成员 4 待补 / 不再绕过的边界；关联 [适配分工决策表](./前后台适配分工决策表.md) 与 [适配实现指南](./成员4向低代码适配实现指南.md) |
