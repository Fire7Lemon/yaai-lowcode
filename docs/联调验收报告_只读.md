# 联调验收报告（只读）

> **生成时间**：2026-05-30
> **检查方式**：只读 —— 阅读 docs + 只读扫描 `yaai-lowcode/src` 与 `YAAI/src` + 后端 9876 **仅 GET** + 启动两端 dev 服务并 **仅 GET** 探测。
> **未做**：未改任何代码、未改 `YAAI/`、未改 `yaai-lowcode/src`、未改后端、未执行 SQL、未调用 POST/PUT/PATCH/DELETE、未 `npm install`、未改 env/vite 配置、未自动修复、未格式化。
> **关联**：[成员3实现进度检查报告.md](./成员3实现进度检查报告.md)、[成员4前台联调数据交接清单.md](./02_前台对接/成员4前台联调数据交接清单.md)、[后端待修复清单.md](./04_后端修复/后端待修复清单.md)。

---

## 1. 本轮权限边界

| 约束 | 执行结果 |
|------|----------|
| 不改任何代码 / `YAAI/` / `yaai-lowcode/src` / 后端 | ✅ |
| 不执行 SQL 写 | ✅ |
| 不调用后端写接口（POST/PUT/PATCH/DELETE） | ✅ 仅 GET |
| 不自动修复 / 不格式化 / 不 `eslint --fix` / `prettier --write` | ✅ |
| 不 `git add/commit/checkout/restore` | ✅ |
| 不 `npm install`、不改 env / vite 配置 | ✅ |
| 仅允许写本报告 | ✅ 仅写 `yaai-lowcode/docs/联调验收报告_只读.md` |

> 启动了两端本地 dev 服务（规则 10、第六部分明确允许），仅用于 GET 探测；未做任何写操作。

---

## 2. 文档理解摘要

1. **成员3职责**：输出稳定的后台低代码数据与契约（菜单/页面/版本/平铺节点树/组件定义/数据绑定）+ 维护后台管理端前端与低代码编辑器；适配集中在 `src/api/*`，类型/表单 snake_case，JSON 字段 `string|null`，响应 camelCase。
2. **成员4职责**：实现前台动态渲染运行时（node-tree API、flat→tree、NodeRenderer、component-map、UnknownComponent、RichTextBlock、DynamicPage、路由放开）。
3. **消费链路**：`GET /menus`→`GET /pages`→path 匹配→`currentVersionId`→`GET /page-versions/{id}/node-tree`（camelCase 平铺）→flat→tree→`NodeRenderer`+`component-map`→未知键兜底；`propsJson` 运行时 `safeJsonParse`。
4. **可用 page/path/cvid/node-tree**：5 页 7~11 → `/`(308)、`/news`(309)、`/services`(310)、`/conference`(311)、`/about/introduction`(312)；308=9 节点，309~312 各 2 节点。
5. **component_key 白名单（10）**：page_container、hero_banner、notice_list、quick_links、home_services、home_events、news_list、home_shortcuts、friend_links、rich_text；禁用别名 service_matrix/event_list。
6. **联调临时约定**：`currentVersionId` 经 `PUT /pages` 临时绑定；版本全 `draft`；**不依赖 `status===published`**，以「cvid 能拉到 node-tree」为准；node-tree 一律 camelCase。
7. **后端待修复**：`POST .../publish`（SYSTEM_ERROR，⭐⭐⭐）、`GET /page-versions/{id}` 详情、menu move、component-def by-key/delete、node move sortOrder、copy parentId、整树 snake_case PUT 毁树/id_map、列表分页字段不统一。
8. **成员3是否还有必须先修的问题**：**无阻塞前台联调的必修项**；唯一代码侧缺口为 `page-version.ts` 版本管理（create/delete/publish/clone/lock/unlock）真实分支缺失（P1，不阻塞前台只读消费）。

---

## 3. 工作区状态

> 工作区根 `D:\procedure\HTML\yaai-work` **不是** git 仓库；`YAAI/`、`yaai-lowcode/` 各自为独立 git 仓库。

| 检查 | 结果 |
|------|------|
| `YAAI/` 改动 | **无**（`git -C YAAI status --short` 为空） |
| `yaai-lowcode/src` 改动 | **无** |
| docs 改动 | 仅 1 个未跟踪文件 `yaai-lowcode/docs/成员3实现进度检查报告.md`（上一轮报告） |
| 本轮开始前工作树 | 除上一轮报告外干净；本轮新增本报告 |

未执行任何 `git add/commit/checkout/restore`。

---

## 4. yaai-lowcode 代码扫描结果

### 4.1 工程 / 环境

- `package.json`：Vue 3.5 + Vite 7 + Pinia 3 + Element Plus；脚本 `dev/build/preview/type-check`。
- `vite.config.js`：`server.proxy['/api']` → `http://localhost:9876`，`rewrite` 去掉 `/api`。
- **`.env*`：不存在**（无 `.env` / `.env.development` 等）。
  - `shouldUseRealApi()` = `import.meta.env.VITE_USE_REAL_API === 'true'` → **未设置 → 返回 false → 默认 mock 模式**。
  - `getApiBaseUrl()` = `VITE_API_BASE_URL` → 未设置 → 真实模式下 `requestJson` 会抛 `VITE_API_BASE_URL is empty.`。
  - **结论**：`npm run dev` 直接启动时，管理端跑 **mock 数据**（`src/mock/database.ts` 的 2 页/3 版本等），**不会**展示后端真实 5 页/308 数据，除非以 `VITE_USE_REAL_API=true` + `VITE_API_BASE_URL=/api` 启动。

### 4.2 API 真实接入（`shouldUseRealApi()` 为真时是否真发 HTTP、失败是否 throw、写体 camelCase、JSON 字符串）

| 模块 | 真实接入 | 失败 throw（不 fallback mock 掩盖） | 备注 |
|------|:--------:|:----------------------------------:|------|
| page（list/detail/create/update/delete） | ✅ | ✅ | 写体 camelCase；不含 currentVersionId（与契约一致） |
| menu（含 move） | ✅ | ✅ | camelCase |
| component-def（list/get/by-key/create/update/status/delete） | ✅ | ✅ | camelCase；JSON 字符串 |
| data-binding（含 preview） | ✅ | ✅ | camelCase |
| page-node（node-tree GET、create/update/move/delete/copy、整树 PUT） | ✅ | ✅ | update 不含结构字段；move 单独接口；整树 PUT camelCase 平铺 |
| page-template（CRUD + node-tree GET/PUT） | ✅ | ✅ | node-tree PUT 用 `buildSaveNodeTreeRequestBodyForBackend`（camelCase） |
| reusable-fragment（CRUD + node-tree GET/PUT） | ✅ | ✅ | 同上 |
| **page-version** | ⚠️ **部分** | — | **list/get/from-template 真实**；**create/delete/publish/clone/lock/unlock 无 `shouldUseRealApi()` 分支，仅内存 mock** |

### 4.3 编辑器

- `stores/editor.ts`：支持三类 `EditorSourceType`（version/template/fragment）；`loadEditor` 三类 GET；`saveTree` 三类 PUT；version 上下文 create/patch/move/copy/delete 走真实 page-node API 并 reload；template/fragment 本地编辑 + 整树保存；`pickContentUpdatePayload` 过滤结构字段；move 走独立接口；删除真实 DELETE + 不假删。
- `EditorWorkspace.vue`：JSON 字段 `JSON.parse` 校验（`validatePatchJsonFields`）；各操作 try/catch + `ElMessage.error`；保存成功后 `loadEditor` 重载。
- `ComponentPalette.vue`：分「YAAI 推荐（白名单）」与「其他/历史」两组。
- `constants/yaaichannel-component-keys.ts`：`YAAI_OFFICIAL_COMPONENT_KEYS` 与白名单 10 键逐字、顺序一致。
- `mock/database.ts`：含 10 白名单键（+历史 `hero_split_container`），无禁用别名。

### 4.4 关键风险（仅记录）

- **真实模式失败 fallback mock 掩盖**：未发现（各 API 在真实模式失败均 `throw`，仅 `copyPageNode` 有“成功但无 nodes”时重拉 node-tree 的补偿，非掩盖失败）。
- **page-version 操作假成功**：**存在** —— 真实模式下 `createPageVersion/deletePageVersion/publishPageVersion/clonePageVersion/lock/unlock` 仍操作内存 mock，会“假成功”（见 §10 P1-A）。

---

## 5. YAAI 代码扫描结果

> **重大更新**：docs（2026-05-25）记载成员4 node-tree 渲染层“缺失”，**本轮实测已全部落地**。

| 检查项 | 结果 | 文件 |
|--------|------|------|
| `public-node-tree.ts` | ✅ 已有（`fetchPageNodeTree`） | `src/api/public-node-tree.ts` |
| `types/lowcode.ts` | ✅ 已有（camelCase + propsJson `string\|null` + `page_version` snake_case 键） | `src/types/lowcode.ts` |
| `buildNodeTree.ts` | ✅ 已有（flat→tree + 孤儿跳过 warn + `pickRootNode` 多根 warn） | `src/utils/buildNodeTree.ts` |
| `NodeRenderer.vue` | ✅ 递归 + `safeJsonParse` + `onErrorCaptured` 兜底 | `src/components/lowcode/NodeRenderer.vue` |
| `component-map.ts` | ✅ **10 白名单键全覆盖**（8 section + LowcodeContainer + RichTextBlock），未知键 → UnknownComponent | `src/components/lowcode/component-map.ts` |
| `UnknownComponent.vue` | ✅ 已有 | `src/components/lowcode/UnknownComponent.vue` |
| `RichTextBlock.vue` | ✅ 已有 | `src/components/lowcode/RichTextBlock.vue` |
| `LowcodeContainer.vue` | ✅ 已有（page_container 布局壳） | `src/components/lowcode/LowcodeContainer.vue` |
| `DynamicPage.vue` 能 fetch+render | ✅ fetch→`pickRootNode`→`NodeRenderer`，含 loading/error/empty/重试，首页走 `HomeDynamicContent` | `src/views/cms/DynamicPage.vue` |
| `shouldRegisterDynamicRoute` 放开 portal | ✅ 已去掉 `portal→false`；非静态前缀且非 channel/single → 注册 DynamicPage | `src/utils/sitePageNav.ts` |
| 首页 8 section 与 component_key 映射 | ✅ component-map 8 键 → `views/home/sections/*` 8 组件，一一对应 | — |
| propsJson safeJsonParse | ✅ `utils/safeJsonParse.ts` + NodeRenderer 使用 | — |
| 未知 componentKey 兜底 | ✅ `getComponentByKey` 找不到 → UnknownComponent；不抛错 | — |
| 是否依赖 `version.status==='published'` | ✅ **不依赖**（DynamicPage 仅看 `currentVersionId` 能否拉到 node-tree） | — |
| 路由实际承接 | 全 5 portal 页（含 `/`、`/services`、`/conference`）均注册到 DynamicPage；`router/index.ts` 静态路由仅余 `/content`、`/apply`、`/login`、`/user` | `stores/sitePages.ts` `registerSiteRoutes` |
| envelope 处理 | `utils/request.ts` 拦截器自动解 `success`，`success=false` → ElMessage + reject；401 跳登录 | — |

- `.env`：仅 `YAAI/.env.example`（`VITE_API_BASE_URL=/api`），**无实际 `.env`** → 默认启动时 `VITE_API_BASE_URL` 未定义（见 §7、§10 P0-环境）。
- 备注：`views/home/Index.vue`（静态 8 section 聚合）仍在仓库，但 `createBuiltinModuleRoute` 对 portal 返回 null，`/` 实际走 DynamicPage → `HomeDynamicContent`（渲染 308），静态 Index 已无路由引用（遗留兜底）。

---

## 6. 后端 GET 接口检查结果

> 后端 `127.0.0.1:9876` **在线**（首次请求冷启动超时，复测 HTTP 200，TCP 端口可达）。以下均为 GET。

| 接口 | success | 关键结果 | 是否符合预期 |
|------|:-------:|----------|:------------:|
| `GET /menus?current=1&size=100` | true | total=**5**；id 9~13，全 `urlType=page`、`externalUrl=null`，pageId 7/11/8/9/10，sortOrder 10~50 | ✅ |
| `GET /pages?current=1&size=100` | true | total=**5**；`/`→308、`/news`→309、`/services`→310、`/conference`→311、`/about/introduction`→312；全 portal、status=true | ✅ |
| `GET /component-defs?current=1&size=100` | true | total=**16**；白名单 **10 键全部 present + status=true** | ✅ |
| `GET /data-bindings?current=1&size=100` | true | total=**3**；id 1/2/3 → `news_category_xhxw`/`home_banner`/`notice_category`，status=true | ✅ |
| `GET /page-versions/308/node-tree` | true | nodes=**9**，keys=page_container+hero_banner/notice_list/quick_links/home_services/home_events/news_list/home_shortcuts/friend_links | ✅ |
| `GET /page-versions/309/node-tree` | true | nodes=**2**（page_container+rich_text） | ✅ |
| `GET /page-versions/310/node-tree` | true | nodes=**2**（page_container+rich_text） | ✅ |
| `GET /page-versions/311/node-tree` | true | nodes=**2**（page_container+rich_text） | ✅ |
| `GET /page-versions/312/node-tree` | true | nodes=**2**（page_container+rich_text） | ✅ |

**结论**：后端 GET 全链路连通且与交接清单 / 数据库直连只读检查 **逐项一致**。

---

## 7. 前端服务启动检查结果

| 项 | 结果 |
|----|------|
| `node_modules` | 两端均存在（未 `npm install`） |
| yaai-lowcode `npm run dev` | ✅ 启动成功，`http://localhost:5174/`（5173 被占用自动改 5174）；`GET /` → HTTP 200 text/html；`GET /api/pages` 经代理 → HTTP 200 **application/json** |
| YAAI `npm run dev` | ✅ 启动成功，`http://localhost:5173/`；`GET /` → HTTP 200 text/html；`GET /api/menus` 经代理 → HTTP 200 **JSON（真实后端数据）** |
| 端口冲突 | 仅记录：5173 被占用，lowcode 落到 5174；未改配置 |
| 缺依赖/编译错误 | 无（Vite ready，无 error） |

**关键实测（env 影响）**：
- YAAI dev `GET /api/menus` → 返回真实后端 JSON（**代理正常**）。
- YAAI dev `GET /menus`（**应用在 `VITE_API_BASE_URL` 未设置时实际发出的路径**）→ 返回 **text/html（SPA fallback）**，非 JSON。
- 即：**无 `.env` 时，YAAI 应用 axios `baseURL=undefined`，API 调用打到 `/menus` 命中 SPA 回退页而非后端**，浏览器联调会失败（拦截器报“服务异常”）。这是**环境/配置前置**，复制 `.env.example` 为 `.env`（`VITE_API_BASE_URL=/api`）即可解决。

---

## 8. 浏览器/Network 验收结果

> 本环境**无浏览器自动化工具**，未做真实点击/截图渲染验证。以下为「实际启动 dev + GET 探测 + 代码扫描」推断，**不声称已可视化验证**。

- **是否实际启动**：是（YAAI :5173、lowcode :5174 均 ready）。
- **是否实际打开页面（浏览器渲染）**：否（无浏览器工具）。
- **Network 推断**：
  - 代理层已实测可用：`/api/menus`、`/api/pages`、`/api/page-versions/{308..312}/node-tree` 经 dev server 代理均能拿到真实 JSON。
  - 但 **YAAI 应用层默认 `baseURL` 未配置**，实际会请求 `/menus`（非 `/api/menus`）→ SPA 回退 → 解析失败。**需先配置 `.env` 才能在浏览器内联调。**
- **Console / 白屏 / 兜底 / rich_text**：代码层具备 `safeJsonParse` 容错、`UnknownComponent` 兜底、`onErrorCaptured` 单节点失败不拖垮整页、`RichTextBlock` 渲染 309~312；但**实际浏览器表现未验证**（受 §7 env 前置影响）。
- **lowcode 管理端**：默认 mock 模式 → 页面管理显示 **2 条 mock 页面**、版本显示 mock（101/102/201），**非** 后端 5 页/308；需 `VITE_USE_REAL_API=true` 才显示真实数据。

---

## 9. 全链路验收表

| 验收项 | 结果 | 证据 | 问题归属 |
|--------|:----:|------|----------|
| 1 后端 9876 GET 连通 | ✅ | TCP 9876 可达；复测 HTTP 200 | — |
| 2 menus 可读 | ✅ | success=true,total=5 | — |
| 3 pages 可读 | ✅ | success=true,total=5 | — |
| 4 currentVersionId 可读 | ✅ | 7→308…11→312 | — |
| 5 308 node-tree 可读 | ✅ | nodes=9 | — |
| 6 309~312 node-tree 可读 | ✅ | 各 nodes=2 | — |
| 7 component_def 白名单可读 | ✅ | 16 条，10 键全 status=true | — |
| 8 data_binding 可读 | ✅ | 3 条，id 1/2/3 | — |
| 9 yaai-lowcode 管理端可启动 | ✅ | dev ready :5174 | — |
| 10 YAAI 前台可启动 | ✅ | dev ready :5173 | — |
| 11 `/about/introduction` 进 DynamicPage | ⚠️ 代码就绪，运行未验证 | `shouldRegisterDynamicRoute` 放开 + `registerSiteRoutes` 注册 SitePage_11 | 环境（需 .env） |
| 12 `/news` 进 DynamicPage | ⚠️ 代码就绪，运行未验证 | 同上（SitePage_8） | 环境（需 .env） |
| 13 rich_text 可渲染 | ⚠️ 代码就绪，运行未验证 | `RichTextBlock` + 309~312 数据齐 | 环境（需 .env） |
| 14 首页可正常显示 | ⚠️ 代码就绪，运行未验证 | `/`→DynamicPage→`HomeDynamicContent`(308) | 环境（需 .env） |
| 15 component-map 覆盖 10 白名单 | ✅ | `component-map.ts` 10 键 | — |
| 16 UnknownComponent 存在 | ✅ | `components/lowcode/UnknownComponent.vue` | — |
| 17 propsJson parse 容错 | ✅ | `utils/safeJsonParse.ts` + NodeRenderer | — |
| 18 是否仍受 publish/draft 临时方案影响 | ⚠️ 是（不阻塞） | 全版本 draft；前台不依赖 status；cvid 经 PUT 绑定 | 后端（publish 必修） |

---

## 10. 问题分级与责任归属

### P0（阻塞成员4前台“浏览器内”联调）

**P0-环境｜两端缺 `.env`，YAAI 默认 `VITE_API_BASE_URL` 未配置**
- 描述：仓库无实际 `.env`（YAAI 仅 `.env.example`，lowcode 无）。YAAI 应用 axios `baseURL=undefined` → API 调 `/menus` 命中 SPA 回退（实测 text/html），无法到达后端；lowcode 默认 mock 模式不读后端。
- 证据：`Get-ChildItem .env*` 仅 `YAAI/.env.example`；dev 实测 `GET /menus`→text/html、`GET /api/menus`→真实 JSON；`utils/request.ts` `baseURL: import.meta.env.VITE_API_BASE_URL`；`http-client.ts` `shouldUseRealApi`/`getApiBaseUrl`。
- 归属：**环境/配置**（非成员3/成员4代码缺陷）。
- 是否需立即修：**是**（但属配置，非本轮可改——本轮禁止改 env）。
- 建议下一轮：在 YAAI 创建 `.env`（`VITE_API_BASE_URL=/api`）；在 lowcode 创建 `.env`（`VITE_USE_REAL_API=true`、`VITE_API_BASE_URL=/api`）。代理已就绪，配置后即可联调。

> 说明：后端 + 成员3数据 + 成员4运行时代码均已就绪；P0 仅是“起服务前的 env 配置”这一步，不涉及任何代码修改。

### P1（不阻塞前台只读消费，但必须修）

**P1-A｜`yaai-lowcode/src/api/page-version.ts` 版本管理真实分支缺失（假成功风险）**
- 描述：真实模式下 `createPageVersion/deletePageVersion/publishPageVersion/clonePageVersion/lockPageVersion/unlockPageVersion` 无 `shouldUseRealApi()` 分支，仍改内存 mock，会“假成功”。
- 证据：`page-version.ts` 对应函数体直接走 mock；`PageVersionListView.vue` 调用这些函数。
- 归属：**成员3**。
- 是否需立即修：否（不阻塞成员4前台只读消费）；建议下一轮补真实分支（publish 待后端修复后启用）。

**P1-B｜后端 `POST /page-versions/{id}/publish` SYSTEM_ERROR / 全版本 draft / cvid 经 PUT 临时绑定**
- 证据：`后端待修复清单.md` §10；`数据修正执行记录.md` 第四/五轮。
- 归属：**后端**（publish 必修）；联调期由临时方案兜住，前台不依赖 status。
- 是否需立即修：正式上线前必修；联调期不阻塞。

### P2（后续优化）

- **P2-1**：docs（02_前台对接）仍描述成员4 渲染层“缺失”，与 YAAI 实测（已落地）不同步 → 仅文档修订，归属成员3/4 协同。
- **P2-2**：列表分页字段不统一（pages=`records`、menus=`items`）→ 后端，已前端双兼容，不阻塞。
- **P2-3**：309~312 `rich_text` 为占位文案、`page.created_at/updated_at` 为 NULL → 后续打磨。
- **P2-4**：lowcode 无 `.env.example`，新人易忽略真实模式开关 → 建议补 `.env.example`（归属成员3）。

---

## 11. 当前是否可以交给成员4继续联调

**可以，且成员4运行时代码已基本完成。** 后端 GET 全链路就绪、成员3数据冻结且一致、成员4 DynamicPage/NodeRenderer/component-map/路由全部落地、两端 dev 可启动、代理可用。

**唯一前置**：需先创建 `.env`（环境配置，非代码）。配置后即可在浏览器完成第一阶段联调验收。

- 是否可继续交给成员4：**是**（剩“浏览器实跑验收”，代码无需重写）。
- 是否需要成员3先修：**否**（P1-A 可下一轮，非阻塞）。
- 是否需要后端先修：联调期**否**；正式上线前需修 publish。
- 是否需要先配环境：**是**（创建 `.env`）。

---

## 12. 下一轮建议

> 唯一建议：**先配置环境（创建 `.env`）后启动两端做浏览器联调验收**。

具体：
1. YAAI：复制 `.env.example` 为 `.env`（`VITE_API_BASE_URL=/api`）。
2. lowcode：新建 `.env`（`VITE_USE_REAL_API=true`、`VITE_API_BASE_URL=/api`）。
3. 启动后端 9876 + 两端 dev，按 `成员4向低代码适配实现指南.md` §8 八项在浏览器验收（5 页可达、5 个 node-tree 请求成功、未知键兜底、JSON 容错、首页 308 渲染）。

> 次选（非本轮）：成员3 下一轮补 `page-version.ts` 真实分支（P1-A）。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-30 | 首版：只读联调验收。后端 GET 全链路通过；成员4 YAAI 运行时已落地；定位唯一前置为「缺 `.env` 环境配置」；记录 page-version 假成功风险（P1-A） |
