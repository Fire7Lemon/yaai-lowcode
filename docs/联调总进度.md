# 联调总进度

最后更新：2026-05-03（阶段 **G**：最终冒烟 + 文档收口 + 后端交付物）

说明：`npm run type-check` 最近一次：**通过**（阶段 G **仅 `docs`**）。

## 模块状态总览

| 模块 | 状态 | 备注 |
|------|------|------|
| page | 已收口 | 真实接入与闭环已完成 |
| menu | 阻塞：后端问题 | `POST /menus/{id}/move` 仍为 SYSTEM_ERROR；前端失败提示，不假成功 |
| component-def | 阻塞：后端问题 | `GET .../by-key/{key}`、`DELETE .../{id}` SYSTEM_ERROR；前端不假删、不 mock 掩盖 |
| data-binding | UI 已自测 | 列表/表单/预览/删除错误处理已按联调要求收口 |
| page-template | 已接入真实接口 | CRUD + node-tree API 层适配；列表/表单 UI 错误处理已加固 |
| reusable-fragment | 已接入真实接口 | 本轮补齐列表删除与表单加载/保存 try·catch + ElMessage |
| page-version | 已接入真实接口（部分阻塞） | 列表/创建/发布/复制/锁定/删除 UI 错误处理已加固；**详情接口后端异常** |
| page-node / node-tree（API 与类型） | **已收口** | 单节点与三类整树 save **camelCase**；详见 **`docs/后端待修复清单.md` §5～§9** |
| editor — **页面版本** | **已收口（代码）** | **`/pages/:pageId/versions/:versionId/editor`**；保存后 **`loadEditor`**；需浏览器最终验收 |
| editor — **模板** | **已收口（代码）** | **`/page-templates/:id/editor`**；**`saveTemplateNodeTree`**；保存后 **`loadEditor`**；需浏览器验收 |
| editor — **片段** | **已收口（代码）** | **`/reusable-fragments/:id/editor`**；**`saveFragmentNodeTree`**；同上 |
| **最终状态总述** | **前端代码层已收口** | **后端阻塞项**见 **`docs/后端待修复清单.md`**；**全后台浏览器冒烟**尚未执行（阶段 G 未跑 dev） |
| 全后台冒烟（浏览器） | **待人工** | 依赖本地 **`npm run dev`** + 后端 **9876** |

## 详细状态

### page
- **状态**：已收口（历史完成）

### menu
- **状态**：阻塞：后端问题（move）

### component-def
- **状态**：阻塞：后端问题（by-key、DELETE）

### data-binding
- **状态**：UI 已自测（阶段 A 完成）

### page-template（阶段 B）
- **状态**：已接入真实接口；UI 列表/表单最小错误处理已完成
- **E-6.1**：直连 **`127.0.0.1:9876`** 时列表为 **`GET /page-templates`**（**不要** 再加 **`/api`**）；本机 **`success:true`**，**`data.records`**；**`GET /page-templates/{id}/node-tree`** 只读可用（示例 **`templateId=1`**：7 节点，平铺 camelCase）。
- **E-6.2**：**curl** 新建专用模板 **`templateId=4`**（**code** **`node_tree_template_test_1777801913836`**），初始 **`nodes: []`**；**camelCase** **`PUT …/page-templates/4/node-tree`**（单根 **`id:null`**）**`success:true`**，返回 **`data.nodes`**（**1** 条）、**`parentId:null`**、**`templateId:4`**；再次 **GET** 一致；**`DELETE …/page-templates/4`** 成功，**GET** 详情 **`PAGE_TEMPLATE_NOT_FOUND`**。**未改动 `src`**。
- **E-7**：列表 **「编辑器」** → **`/page-templates/:id/editor`**；**`saveTree`** → **`saveTemplateNodeTree`** → **`buildSaveNodeTreeRequestBodyForBackend`**；保存失败 **`throw`** + **`ElMessage.error`**；保存成功后 **`loadEditor`** 与 **页面版本** 行为对齐。**本地启动前端后** 按 **`docs/前端联调修复报告.md` E-7** 清单做 Network 验收。

### reusable-fragment（阶段 C）
- **状态**：已接入真实接口；本轮完成 Fragment 列表/表单与模板页面对齐的错误处理
- **E-6.1**：直连列表为 **`GET /reusable-fragments`**（**不要** **`/api`**）；本机 **`success:true`**，**`data.records`**；**`GET /reusable-fragments/{id}/node-tree`** 只读可用（示例 **`fragmentId=1`**：2 节点）。
- **E-6.2**：**curl** 新建专用片段 **`fragmentId=4`**（**code** **`node_tree_fragment_test_1777801913836`**），初始 **`nodes: []`**；**camelCase** **`PUT …/reusable-fragments/4/node-tree`** 单根保存 **成功**，再次 **GET** 一致；**`DELETE …/reusable-fragments/4`** 成功，**GET** **`FRAGMENT_NOT_FOUND`**。**测试数据无遗留**。
- **E-7**：列表 **「编辑器」** → **`/reusable-fragments/:id/editor`**；**`saveTree`** → **`saveFragmentNodeTree`**；保存后 **`loadEditor`**。**浏览器验收** 同模板。

### page-version（阶段 D）
- **状态**：已接入真实接口；列表侧操作均有失败提示与刷新
- **阻塞：后端问题**：`GET /page-versions/{id}` 返回 `success:false` + `SYSTEM_ERROR`（curl 与本机 9876 已复现），影响依赖详情的页面（如版本详情、编辑器首屏若拉详情）

### page-node / 编辑器（阶段 E）
- **E-1**：代码扫描与三类 GET node-tree 只读探测（历史结论保留）。
- **E-2**：`src/api/page-node.ts` 单节点真实分支 + `fetchRealVersionNodeTree` 信封校验（历史）。
- **E-3**：**`src/stores/editor.ts`** + **`EditorWorkspace.vue`**（单节点 HTTP 对齐与首批 **`ElMessage`**）
  - **页面版本**（`sourceType === 'page_version'` 且节点 **`id > 0`**）：**`deleteNode`** → **`deletePageNode`**，成功后 **`loadEditor`** 重拉树；失败 **throw**，不假删。
  - **`createEditorNode`**：`page_version` → **`createPageNode`** + **`loadEditor`**；模板/片段仍为 **`createNodeLocally`**。
  - **`patchNode`**：仅 **`pickContentUpdatePayload`**（内容字段），不调结构字段；成功后合并 **`updatePageNode`** 返回节点。
  - **`moveNode`**：**`movePageNode`** 后 **`loadEditor`**，返回值取 **重拉后的 `nodes`**（避免沿用可能滞后的 API 返回）；若树上无该 id → **`undefined`** → UI 提示失败。
  - **`copyNode`**：维持 **`copyPageNode`** + **`loadEditor`**；返回值优先树上 **`find`**。
  - **模板 / 片段**：删除仍为 **`deleteNodeLocally`**（无单节点 DELETE 接入约定）；依赖后续整树保存。
- **E-4（本轮）**：**`EditorWorkspace.vue`** 最小加固（未跑 dev，浏览器 UI 由人工按清单验收）
  - **加载失败**：`load()` **`try/catch`** + **`ElMessage.error`**。
  - **非法 JSON**：属性 **`patch`** 前校验 **`props_json` / `style_json` / `layout_json` / `event_json` / `visible_rule_json`**（非空串须 **`JSON.parse`** 通过）。
  - **保存整树**：**`catch`** 展示 **`Error.message`**；**E-7** 起三种 **`sourceType`** 保存成功后均 **`loadEditor`** 再对齐服务端树（失败则 **`ElMessage.warning`**）。
  - **curl（versionId=307）**：create → update → move → copy → 删副本 → 删原节点均已探测；**move** 仍见 **sortOrder** 未按请求变化；**copy** 响应曾出现 **`parentId: null`**（见后端清单 §7）。
  - **component-def by-key**：编辑器依赖 **`componentStore.load()` 列表**，非 **`by-key`**；**by-key** 仍后端异常时不影响列表路径。
- **E-5（本轮）**：**`PUT /page-versions/307/node-tree`** curl 闭环
  - **camelCase** 原样 **`data.nodes`**：**success**，返回 **`data.nodes` + `data.id_map`**；再次 GET **节点数仍为 7**，层级正确（单根 + 父子链）。
  - **snake_case**（模拟旧前端 **`JSON.stringify(payload)`**）：**success** 但响应中 **全部 `parentId` 为空**，树被**扁平破坏**；已用 **camelCase** 备份体把 **307** 树恢复。
  - **前端修复**：**`saveVersionNodeTree`** 真实模式改为发送 **`buildSaveNodeTreeRequestBodyForBackend`**（原名 **`buildSaveVersionNodeTreeRequestBodyCamel`**，**E-6** 已导出并复用）（**不改** store / **`buildSavePageNodeTreePayload`** 的 snake_case 语义）。
  - **后端观测**：每次成功 PUT 可能返回 **`id_map`** 重映射节点 **id**（稳定外链风险见 **`docs/后端待修复清单.md` §9**）。
- **E-6（本轮）**：模板 / 片段整树 PUT 与版本对齐
  - **`saveTemplateNodeTree` / `saveFragmentNodeTree`** 真实分支由 **`JSON.stringify(payload)`**（snake_case）改为 **`JSON.stringify(buildSaveNodeTreeRequestBodyForBackend(payload))`**，与 **`saveVersionNodeTree`** 共用 **`src/api/page-node.ts`** 构造函数。
  - **依据**：**E-5** 已证实 **`snake_case`** 整树 PUT 虽 **`success:true`** 但 **`parentId`** 绑定丢失、毁树；模板 / 片段路径此前同为 snake_case 直传，**推断同类风险**，前端先行对齐 camelCase。
  - **验证（E-6，路径有误）**：曾用 **`GET http://127.0.0.1:9876/api/page-templates`**（及片段 **`/api/reusable-fragments`**）得到 **`SYSTEM_ERROR`**，**误判**为列表不可用。
- **E-6.1（本轮）**：直连后端路径复核（**不改 `src`**，仅 curl + 文档）
  - **`GET http://127.0.0.1:9876/page-templates`**：HTTP **200**，**`success:true`**，**`data.records`**（本机 3 条，**id** `1`～`3`）。
  - **`GET http://127.0.0.1:9876/api/page-templates`**：HTTP **200**，**`success:false` `SYSTEM_ERROR`**，`data:null` — **非** 业务列表路径，**curl 直连时应省略 `/api`**。
  - **片段**对称：**`/reusable-fragments`** 成功；**`/api/reusable-fragments`** 同上 **`SYSTEM_ERROR`**。
  - **只读 node-tree**：**`GET …/page-templates/1/node-tree`**、**`GET …/reusable-fragments/1/node-tree`** 均 **`success:true`**，**`data.nodes`** 平铺，camelCase，JSON 字段字符串或 **null**。**本轮未执行 PUT**。
- **E-6.2（本轮）**：**curl** 直连 **`127.0.0.1:9876`**（**无 `/api`**），专用 **POST** 创建模板/片段 → **GET** **`nodes`**（均为 **`[]`**）→ **camelCase** **`PUT …/node-tree`**（**`{ nodes }`** 平铺）→ **GET** 校验 → **DELETE** 清理；详见 **`page-template` / `reusable-fragment`** 小节。**未修改 `src`**。
- **E-7（本轮）**：**`EditorWorkspace.vue`** **`handleSave`**：模板 / 片段保存成功后增加 **`loadEditor`**（此前仅 **页面版本** 重拉，与页眉提示不一致）。**`editorStore.saveTree`** 已调用 **`saveTemplateNodeTree` / `saveFragmentNodeTree`**（API 层 **`buildSaveNodeTreeRequestBodyForBackend`**）。**未运行 dev**；人工 UI 验收见 **`docs/前端联调修复报告.md`**。

### 低代码编辑器整体（阶段 F）
- **状态**：代码侧已并入 **E-7**；**浏览器 UI** 验收并入 **阶段 G 人工冒烟**

### 冒烟（阶段 G）
- **curl 直连** **`127.0.0.1:9876`**（**无 `/api`**），**`--max-time 10`**，**只读 / 非破坏性**（**2026-05-03**）：

| 请求 | HTTP | success | data 形态摘要 |
|------|------|---------|----------------|
| **`GET /pages?pageNo=1&pageSize=20`** | 200 | true | **`data.records`**，total=5 |
| **`GET /menus?pageNo=1&pageSize=50`** | 200 | true | **`data.items`**，total=5 |
| **`GET /component-defs?pageNo=1&pageSize=50`** | 200 | true | **`data.records`**，total=12（当前页 10 条） |
| **`GET /data-bindings?pageNo=1&pageSize=50`** | 200 | true | **`data.records`**，total=3 |
| **`GET /page-templates?pageNo=1&pageSize=50`** | 200 | true | **`data.records`**，total=3 |
| **`GET /reusable-fragments?pageNo=1&pageSize=50`** | 200 | true | **`data.records`**，total=3 |
| **`GET /pages/1/versions?pageNo=1&pageSize=20`** | 200 | true | **`data.records`**（含 **307**） |
| **`GET /page-versions/307/node-tree`** | 200 | true | **`data.nodes`** 平铺 **7** 条 + **`page_version`** |
| **`GET /page-templates/1/node-tree`** | 200 | true | **`data.template` + `nodes`**（**7**） |
| **`GET /reusable-fragments/1/node-tree`** | 200 | true | **`data.fragment` + `nodes`**（**2**） |

- **超时**：未超时。**SYSTEM_ERROR**：上表接口本轮均未出现。  
- **交付物**：**`docs/后端接口修复要求_交付版.md`**、**`docs/成员3后台前端联调交付报告.md`**。

### G — 联调遗留测试数据（只读核对）

- **`GET /pages`**：**`id=4`**（**code** `pg_1777793968739_fwmcy91f`）、**`id=5`**（**code** `integration_test_page_1777794000723`，备注「后续可删除」）。**本轮未删除**。  
- **模板 / 片段**：列表无 **`node_tree_*_test`** / **`editor_ui_test`** 等关键词（**E-6.2** 专用资源已删）。  
- **版本**：列表可见 **integration / api-test** 等历史版本（如 **307**）。

## 需用户人工验收项（摘要）

1. 浏览器下 data-binding 预览空数组、删除失败提示、保存跳转。
2. page-template / reusable-fragment 节点树读写与错误提示。
3. page-version 列表各类操作；**版本详情**仍依赖 `GET /page-versions/{id}` 修复；**页面编辑器进版**当前主要依赖 node-tree GET，与详情接口解耦。
4. 后端修复 menu move、component-def by-key/DELETE、page-version detail、**page-node move sortOrder**、**page-node copy 父节点**（§7）、**整树 PUT snake_case**（§8）、**id_map 换 id**（§9）后的回归。
5. 浏览器打开 **`/pages/1/versions/307/editor`**（dev 端口以本地为准）：对照 **E-4** Network 清单完成页面版本编辑器冒烟。
6. 浏览器验证 **`PUT /api/page-versions/307/node-tree`**：请求体 **`nodes`** 为 camelCase 平铺数组且保存后 **`loadEditor`** 树正常（与 **E-5** curl 结论一致）。
7. 将 **`docs/后端接口修复要求_交付版.md`** 提交后端；阅读 **`docs/成员3后台前端联调交付报告.md`**。
