# 后台管理端与后端联调收口报告

> **生成时间**：2026-05-30
> **范围**：成员3 `yaai-lowcode` 后台管理端 × 后端 `127.0.0.1:9876` 真实联调状态收口；明确「哪些能点、哪些不能点」。
> **方式**：只读 —— 读文档 + 读 `yaai-lowcode/src` + 后端**仅 GET** + 确认 dev 在运行；**未做可视化浏览器验收**（本环境无浏览器自动化工具，结论基于 curl + 代码扫描推断，不编造）。
> **未做**：未改 `YAAI/` / `yaai-lowcode/src` / 后端；未执行 SQL；未调用任何写接口（POST/PUT/PATCH/DELETE）；未点击任何 UI 写操作。
> **关联**：[后端待修复清单.md](./04_后端修复/后端待修复清单.md)、[接口字段差异记录.md](./01_设计基线/接口字段差异记录.md)、[数据修正执行记录.md](./05_数据库清洗/数据修正执行记录.md)、[联调验收报告_浏览器.md](./联调验收报告_浏览器.md)、[成员3实现进度检查报告.md](./成员3实现进度检查报告.md)。

---

## 1. 本轮权限边界

| 约束 | 执行结果 |
|------|----------|
| 不改 `YAAI/` / `yaai-lowcode/src` / 后端 | ✅ |
| 不执行 SQL、不调用写接口、不 DELETE、不整树 PUT | ✅ 仅 GET |
| 不新增/改/删 page/menu/page_node/component_def/data_binding | ✅ |
| 不点击 UI 保存/删除/发布/复制/锁定/移动 | ✅（未做浏览器点击） |
| 仅允许写 `后台管理端与后端联调收口报告.md` | ✅ |

---

## 2. 依据文档与已知背景

- **当前后台低代码数据状态**：pages=5（7~11）、menus=5（9~13）、page_version=5（308~312，全 `draft`）、page_node=17（308 九节点 + 309~312 各 2 节点）、component_def=16（白名单 10 全启用、test id=12 停用）、data_binding=3。309~312 已于第九/十轮从 `rich_text` 占位升级为可交付展示态（`news_list`/`home_services`/`home_events`/`rich_text` 正式文案）。
- **当前已知后端阻塞**（[后端待修复清单.md](./04_后端修复/后端待修复清单.md)）：`publish` SYSTEM_ERROR（§10）、`GET /page-versions/{id}` 详情 SYSTEM_ERROR（§4）、`menu move`（§1）、`component-def by-key`（§2）/`delete`（§3）、`page-node move` sortOrder 未持久化（§6）、`copy` parentId 异常（§7）、snake_case 整树 PUT 毁树（§8，已由前端 camelCase 规避）。
- **当前成员3代码缺口**：`src/api/page-version.ts` 的 `create/delete/publish/clone/lock/unlock` 真实模式**仍走内存 mock**（假成功）。
- **本轮联调检查范围**：后台管理端 8 模块 + 编辑器 × 后端 GET 真实联调状态分级（A 可交付 / B 谨慎 / C 禁用）。

---

## 3. 后端 GET 接口检查结果

> 后端在线；以下均 `--max-time 10` GET，`success` 均为 `true`。

| 接口 | HTTP/success | data 摘要 | 与联调数据一致 |
|------|:------------:|-----------|:--------------:|
| `GET /pages?...` | 200 / true | total=**5** | ✅ |
| `GET /menus?...` | 200 / true | total=**5** | ✅ |
| `GET /component-defs?...` | 200 / true | total=**16**（白名单 10 全在） | ✅ |
| `GET /data-bindings?...` | 200 / true | total=**3** | ✅ |
| `GET /page-templates?...` | 200 / true | total=**0**，records=[] | ✅（未使用模板） |
| `GET /reusable-fragments?...` | 200 / true | total=**0**，records=[] | ✅（未使用片段） |
| `GET /pages/7/versions` | 200 / true | 1 条 **v308:draft** | ✅ |
| `GET /pages/8/versions` | 200 / true | 1 条 **v309:draft** | ✅ |
| `GET /pages/9/versions` | 200 / true | 1 条 **v310:draft** | ✅ |
| `GET /pages/10/versions` | 200 / true | 1 条 **v311:draft** | ✅ |
| `GET /pages/11/versions` | 200 / true | 1 条 **v312:draft** | ✅ |
| `GET /page-versions/308/node-tree` | 200 / true | 9 节点（page_container + 8 业务键） | ✅ |
| `GET /page-versions/309/node-tree` | 200 / true | 2 节点（page_container + **news_list**） | ✅ |
| `GET /page-versions/310/node-tree` | 200 / true | 2 节点（page_container + **home_services**） | ✅ |
| `GET /page-versions/311/node-tree` | 200 / true | 2 节点（page_container + **home_events**） | ✅ |
| `GET /page-versions/312/node-tree` | 200 / true | 2 节点（page_container + **rich_text**） | ✅ |

**结论**：所有 GET 链路连通，数据与第十轮升级后状态一致；全 `draft`（联调约定）。

---

## 4. yaai-lowcode API 接入状态

> 判定：`shouldUseRealApi()` 为真时是否真发 HTTP、失败是否 throw（不 fallback mock 掩盖）。`.env.local` 已设 `VITE_USE_REAL_API=true` + `VITE_API_BASE_URL=/api`（dev `:5174` 实测 `/api/pages`→200 JSON）。

| 模块 | 真实接入 | 真实模式失败 throw | 仍走 mock | 备注 |
|------|:--------:|:------------------:|:---------:|------|
| page（list/detail/create/update/delete） | ✅ | ✅ | — | 写体 camelCase；不写 currentVersionId |
| menu（list/detail/create/update/delete/**move**） | ✅ | ✅ | — | move 后端 SYSTEM_ERROR → 前端 throw |
| component-def（list/get/**by-key**/create/update/status/**delete**） | ✅ | ✅ | — | by-key/delete 后端 SYSTEM_ERROR → throw |
| data-binding（list/get/create/update/delete/preview） | ✅ | ✅ | — | — |
| page-node（node-tree GET、create、update、**move**、delete、**copy**、整树 PUT） | ✅ | ✅ | — | 整树 PUT camelCase；move/copy 后端有缺陷 |
| page-template（CRUD + node-tree GET/PUT） | ✅ | ✅ | — | — |
| reusable-fragment（CRUD + node-tree GET/PUT） | ✅ | ✅ | — | — |
| **page-version：list / get / from-template** | ✅ | ✅ | — | 这三个真实接入 |
| **page-version：create/delete/publish/clone/lock/unlock** | ❌ | — | **是（内存 mock 假成功）** | 真实模式下不打后端，会"假成功" |

- **真实模式失败 fallback mock 掩盖**：未发现（各 API 失败均 throw；`copyPageNode` 仅在"成功但无 nodes"时重拉 node-tree 补偿，非掩盖失败）。
- **page-version 假成功风险**：**存在**（见上表最后一行）。

---

## 5. 后台管理端页面只读验收

> **未做可视化浏览器验收**（无浏览器工具）。dev `:5174` 在运行、真实模式开启、`/api` 代理返回真实 JSON；以下为「代码 + 真实 GET」推断，待人工浏览器最终确认。

| 模块 | 推断结论（基于真实 GET + store/view 代码） |
|------|-------------------------------------------|
| 页面管理（PageListView） | 应显示真实 **5** 页，currentVersionId 308~312（`listPages` 真实接入，`/api/pages` total=5）。**勿点新建/编辑保存/删除** |
| 菜单管理（MenuManagementView） | 应显示真实 **5** 菜单，pageId 对应 7~11（`/api/menus` total=5）。**勿点新建/编辑/删除/move（move 后端 SYSTEM_ERROR）** |
| 组件定义（ComponentDefListView） | 应显示 **16** 条，白名单 10 键在，test id=12 停用（`/api/component-defs` total=16）。**勿点启停开关（status PATCH 会真实写）** |
| 数据绑定（DataBindingListView） | 应显示 **3** 条（`/api/data-bindings` total=3）。**勿点删除/预览写** |
| 页面版本（PageVersionListView） | 打开 page 7~11 应见 308~312、状态 **draft**（`/api/pages/{id}/versions` 各 1 条 draft）。**严禁点发布/复制/锁定/删除/新建——真实模式这些走 mock，会假成功（见 §9）** |
| 编辑器（PageEditorView/308 等） | 见 §6 |

---

## 6. 编辑器只读验收

> 同上为推断（未点击）。`editorStore.loadEditor` 对 version 走真实 `getVersionNodeTree`。

| 版本 | 推断节点 | 依据（真实 GET） |
|------|----------|------------------|
| 308 编辑器 | **9** 节点（page_container + hero_banner/notice_list/quick_links/home_services/home_events/news_list/home_shortcuts/friend_links） | `/api/page-versions/308/node-tree` nodes=9 |
| 309 编辑器 | **2** 节点（page_container + news_list） | nodes=2 |
| 310 编辑器 | **2** 节点（page_container + home_services） | nodes=2 |
| 311 编辑器 | **2** 节点（page_container + home_events） | nodes=2 |
| 312 编辑器 | **2** 节点（page_container + rich_text） | nodes=2 |

**只读加载安全**；**勿点保存/移动/删除/复制/新增**（version 上下文这些为真实写：保存/新增/删除/单节点 PUT 会改库；move/copy 后端有缺陷）。

---

## 7. 可交付功能清单（A：可正常使用 / 只读）

- 页面列表只读查看（`/api/pages`）
- 菜单列表只读查看（`/api/menus`）
- 组件定义列表只读查看（`/api/component-defs`，16 条）
- 数据绑定列表只读查看（`/api/data-bindings`，3 条）
- 页面模板 / 可复用片段列表只读查看（均空，正常）
- 页面版本列表只读查看（`/api/pages/{id}/versions`，308~312 draft）
- 编辑器 node-tree 只读加载（308 九节点、309~312 两节点）
- 成员4展示端读取 node-tree（`/api/page-versions/{id}/node-tree`，GET 全通）

---

## 8. 可用但需谨慎功能清单（B：真实写已接入，联调期慎用）

> 真实模式已正确打后端且失败会 throw（不假成功），但属"写操作"，联调期点击会改动联调库数据，**非必要不要点**。

- page 真实 CRUD（create/update/delete）
- menu 真实 create/update/delete（**move 除外，见 C**）
- component-def create/update/**status 启停**（真实 PATCH）
- data-binding 真实 CRUD + preview
- page-node 单节点 **POST（新增）/ PUT（更新内容字段，含 componentKey）**
- editor 整树保存（page_template / reusable_fragment 上下文）与 version 单节点写、删除

> 说明：本会话第九/十轮的 309~312 升级即走 B 类「单节点 PUT」，已验证真实成功且结构不破坏。

---

## 9. 暂不允许使用功能清单（C：禁用）

| 功能 | 原因 | 归属 |
|------|------|------|
| **page-version 发布（publish）** | 真实模式走 mock 假成功；且后端 `publish` SYSTEM_ERROR | 成员3 + 后端 |
| **page-version 新建** | 真实模式走 mock 假成功（不落库） | 成员3 |
| **page-version 删除** | 真实模式走 mock 假成功 | 成员3 |
| **page-version 复制（clone）** | 真实模式走 mock 假成功 | 成员3 |
| **page-version 锁定/解锁（lock/unlock）** | 真实模式走 mock 假成功 | 成员3 |
| **component-def 删除（delete）** | 后端 SYSTEM_ERROR；前端 throw 不假删 | 后端 |
| **component-def by-key 查询** | 后端 SYSTEM_ERROR | 后端 |
| **menu move（移动/排序）** | 后端 SYSTEM_ERROR | 后端 |
| **page-node move（移动）** | 后端 sortOrder 未持久化（操作不报错但顺序不变） | 后端 |
| **page-node copy（复制子树）** | 后端副本 parentId 为空，树层级异常 | 后端 |
| **node-tree snake_case 整树 PUT** | snake_case 会丢 parentId 毁树（前端已 camelCase 规避；勿用原生 snake_case 直连） | 后端 |

---

## 10. 问题分级与责任归属

### P0（阻塞成员4展示端联调）

- **无**。展示端只读 `GET /page-versions/{id}/node-tree`，链路全通，数据已是可交付展示态；component_key 全在白名单。

### P1（不阻塞展示端，但影响后台管理端正式交付）

| # | 问题 | 证据 | 归属 | 建议 | 立即处理 |
|---|------|------|------|------|:--------:|
| P1-1 | page-version `create/delete/publish/clone/lock/unlock` 真实模式走 mock，**假成功** | `src/api/page-version.ts` 对应函数无 `shouldUseRealApi()` 分支 | 成员3 | 下一轮补真实分支（publish 待后端修复后启用）；联调期 UI 禁用这些按钮 | 否（联调期禁用即可） |
| P1-2 | `POST /page-versions/{id}/publish` SYSTEM_ERROR | [后端待修复清单 §10](./04_后端修复/后端待修复清单.md) | 后端 | 修 publish 事务；修复后切回 publish 维护 currentVersionId | 正式上线前必修 |
| P1-3 | `GET /page-versions/{id}` 详情 SYSTEM_ERROR | §4 | 后端 | 修详情接口（编辑器首屏走 node-tree，已解耦） | 否 |
| P1-4 | menu move / component-def by-key、delete SYSTEM_ERROR | §1/§2/§3 | 后端 | 修对应接口 | 否 |
| P1-5 | page-node move sortOrder 未持久化 / copy parentId 异常 | §6/§7 | 后端 | 修 move 持久化与 copy 父指针 | 否 |

### P2（优化 / 文档 / 体验）

- P2-1：version 全 `draft`、currentVersionId 经 `PUT /pages` 临时绑定（待 publish 修复后切换）。
- P2-2：列表分页字段不统一（pages=`records`、menus=`items`）；前端已双兼容。
- P2-3：`home_services`/`home_events`/`news_list`/`home_shortcuts` 等 `propsJson.items` 实际透传到 YAAI 组件为后续 P2（当前组件内置/自取数据，不阻塞）。
- P2-4：page_template / reusable_fragment 当前为空（未使用，非问题）。

---

## 11. 是否影响成员4展示端联调

- **是否影响**：**否**。
- **原因**：展示端只消费只读 `GET /menus`、`GET /pages`、`GET /page-versions/{id}/node-tree`，本轮实测全部 200 + `success=true` + 数据一致；node-tree 已升级为可交付展示态，component_key 全在白名单。后台管理端的 page-version 假成功与后端写缺陷均属"后台写操作"范畴，不触达展示端只读链路。

---

## 12. 下一轮建议

> 唯一建议：**无 P0 → 冻结成员3后台数据，交给成员4做最终展示验收。**

具体：保持后端 9876 + lowcode `:5174`（管理端，仅只读核对）+ YAAI `:5173`（展示端）运行；成员4 在浏览器核对 `/`、`/news`、`/services`、`/conference`、`/about/introduction` 的最终展示。**后台管理端联调期严格遵守 §9 禁用清单**（尤其 page-version 发布/新建/删除/复制/锁定——会假成功）。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-30 | 首版：后台管理端 × 后端 GET 真实联调收口；API 接入分级（A/B/C）；明确 page-version 假成功与后端写缺陷禁用清单；判定无 P0、不影响成员4展示端 |
