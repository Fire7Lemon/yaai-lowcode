# 数据修正执行记录

> **说明**：本条为后台 **HTTP API**（`127.0.0.1:9876`，**无 `/api` 前缀**）受控操作的执行留痕。**页面域清空第一轮**的记录见：**[页面域删除执行记录.md](./页面域删除执行记录.md)**。

---

## 第二轮：component_def 白名单补齐（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-20（本地会话） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | `component_def` 白名单 key 对齐；不写 `page` / `menu` / `page_version` / `page_node`，不 SQL，不触碰 YAAI 与 `yaai-lowcode/src` |

### 允许的写接口（本轮实际使用）

- `GET /component-defs`、`POST /component-defs`、`PATCH /component-defs/{id}/status`
- **curl**：均加 `--max-time 10`
- **重试**：每失败请求至多 **1** 次

### 一、写前 GET

**请求**

```bash
curl.exe --max-time 10 "http://127.0.0.1:9876/component-defs?current=1&size=100"
```

**摘要**

| 项目 | 值 |
|------|-----|
| HTTP | 200 |
| `success` | true |
| `total`（写前） | 12 |
| **已有白名单 key（6）** | `page_container`、`hero_banner`、`notice_list`、`quick_links`、`news_list`、`rich_text` |
| **缺白名单 key（4）** | `home_services`、`home_events`、`home_shortcuts`、`friend_links` |
| **测试 key** | `componentKey=test_component_1777798334444`，`id=12`，`status=true` |

其余 `component_def`（非本轮停用范围）：`container`、`hero_split_container`、`grid_container`、`nav_menu`、`copyright` ——按约定 **保留**。

### 二、写操作结果

#### 2.1 新增（`POST /component-defs`，body camelCase，`propSchemaJson` / `defaultPropsJson` 等为 **字符串**）

| componentKey | 结果 | HTTP | 备注 |
|--------------|------|------|------|
| `home_services` | 成功 | 200，`success=true`，`data.id=13` | 首轮即成功 |
| `home_events` | 成功 | 200，`success=true`，`data.id=14` | |
| `home_shortcuts` | 成功 | 200，`success=true`，`data.id=15` | |
| `friend_links` | 成功 | 200，`success=true`，`data.id=16` | |

无 `DATA_CONFLICT`；无需重复 POST。

#### 2.2 停用测试组件（`PATCH /component-defs/{id}/status`，`{"status":false}`）

| componentKey | id | 写前 status | 操作 | 结果 |
|----------------|-----|-------------|------|------|
| `test_component_1777798334444` | 12 | true | PATCH ×2（含 **1** 次重试） | 均返回 `SYSTEM_ERROR`，`success=false`，**未停用** |

**说明**：遵从约束 **不 DELETE**；失败仅记录，不扩展其它写路径。

### 三、写后 GET 验证

**请求**

```bash
curl.exe --max-time 10 "http://127.0.0.1:9876/component-defs?current=1&size=100"
```

| 校验项 | 结果 |
|--------|------|
| 10 个白名单 key 均存在 | **是**：`page_container`、`hero_banner`、`notice_list`、`quick_links`、`home_services`、`home_events`、`news_list`、`home_shortcuts`、`friend_links`、`rich_text` |
| 10 项 `status=true` | **是** |
| 新增四项 `propSchemaJson` / `defaultPropsJson` 为字符串 | **是**（响应中段内为字符串；服务端可能对键序做规范化） |
| `test_component_1777798334444`（若存在）`status=false` | **否**（仍为 `true`，因 PATCH 失败） |
| `component_def` 总数 | **`total=16`**（写前 12 + 新建 4） |
| **`data_binding` 未被触碰** | **`GET /data-bindings?current=1&size=50` → `total=3`**，与清洗前快照一致预期 |
| **menus / pages / page-templates / reusable-fragments 仍为空（未本轮重建）** | `menus`：`total=0`；`pages`：`total=0`；`page-templates`、`reusable-fragments`：`records=[]`、`total=0` |

---

### 本轮失败项汇总

| 序号 | 操作 | 响应摘要 |
|------|------|-----------|
| 1 | `PATCH /component-defs/12/status` | HTTP 200，body：`code=SYSTEM_ERROR`，`success=false`，「系统繁忙，请稍后再试」×2 |

---

### 未触碰范围声明

- **YAAI**：未修改  
- **`yaai-lowcode/src`**：未修改  
- **SQL**：未执行  
- **page / menu / page_version / page_node**：未调用写接口  
- **`component_def`**：未 DELETE；仅 POST 新增与白名单 PATCH 尝试  

---

### 附录：复核用 GET（非写）

以下为写后顺带核对其它模块仍为「空或未改」的请求（与本轮允许的写接口并列用于验证，不产生写）：

```bash
curl.exe --max-time 10 "http://127.0.0.1:9876/data-bindings?current=1&size=50"
curl.exe --max-time 10 "http://127.0.0.1:9876/menus?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/pages?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/page-templates?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/reusable-fragments?current=1&size=10"
```

---

### 本轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-20 | 第二轮 `component_def` 白名单 POST 补齐四项；PATCH 停用测试组件失败待跟进 |

---

## 第三轮：component_def 测试项停用（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-21（本地会话） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | 仅 **`component_def` id=12**：用 **`PUT /component-defs/12`** 将 **`status`** 置 **`false`**；**不 PATCH**（Round 2 已证该路径不可用）、**不 DELETE** |
| **未触碰范围** | **YAAI**、`yaai-lowcode/src`、**SQL**、**page**、**menu**、**page_version**、**page_node**（无 **`node-tree` PUT**） |

### 允许的写接口（本轮实际使用）

- `GET /component-defs/12`、`PUT /component-defs/12`（camelCase body；字段 **完全来自 GET `data`**，仅 **`status: false`** 与其余一致）
- **curl**：均加 `--max-time 10`
- **重试**：至多 **1** 次（本轮 **PUT 首轮即 `success:true`**，未触发重试）

### 一、写前 GET

**请求**

```bash
curl.exe --max-time 10 "http://127.0.0.1:9876/component-defs/12"
```

**记录**

| 项目 | 值 |
|------|-----|
| HTTP | 200 |
| `success` | true |
| `componentKey` | `test_component_1777798334444` |
| `componentName` | `组件联调测试-已更新` |
| `componentGroup` | `basic` |
| `componentType` | `test_component_xyz` |
| `description` | **响应无此键**（不写入 PUT body） |
| `isContainer` | false |
| `canBindData` | false |
| `propSchemaJson` | 仍为 **JSON 文本字符串**（未解析为请求内嵌对象） |
| `styleSchemaJson` | 字符串 JSON |
| `eventSchemaJson` | `"{}"` |
| `layoutSchemaJson` | `"{}"` |
| `allowedChildTypesJson` | `"[]"` |
| `defaultPropsJson` | 字符串 JSON |
| `defaultStyleJson` | 字符串 JSON |
| `sortOrder` | 1000 |
| `status` | **true**（写操作目标：`false`） |
| `remark` | `组件定义模块联调测试-已更新，后续删除` |
| （GET 附带） | `id`、`icon`、`canReuseAsFragment`、`createdAt`、`updatedAt` 等与 GET **`data`** 完全一致回传 PUT |

### 二、PUT /component-defs/12

- **语义**：复制 GET **`data`** 全字段为请求体，`status` 改为 **`false`**；**未修改 `componentKey`**；未将任何 JSON **字符串字段** 解析为对象或改写其文本内容。
- **结果**：HTTP **200**，`code=SUCCESS`，`success=true`。

### 三、写后 GET 验证

**请求**

```bash
curl.exe --max-time 10 "http://127.0.0.1:9876/component-defs/12"
curl.exe --max-time 10 "http://127.0.0.1:9876/component-defs?current=1&size=100"
curl.exe --max-time 10 "http://127.0.0.1:9876/menus?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/pages?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/page-templates?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/reusable-fragments?current=1&size=10"
curl.exe --max-time 10 "http://127.0.0.1:9876/data-bindings?current=1&size=50"
```

| 校验项 | 结果 |
|--------|------|
| `id=12` **`status`** | **`false`** |
| **10** 白名单 **`component_key` 仍存在且 **`status:true`** | **是** |
| **`menus`** / **`pages`** / **`page-templates`** / **`reusable-fragments`** 仍为「空或未重建」 | **`menus`** **`total=0`**；**`pages`** **`total=0`**；模板/片段列表 **`total=0`** |
| **`data_binding`** **条数** | **`total=3`** |

### 第三轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-21 | 第三轮：测试组件 **`PUT /component-defs/12`** 停用成功 |

---

## 第四轮：page / page_version（草稿骨架）/ menu 重建（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-25（本地会话） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | **`POST /pages`** ×5、**`POST /pages/{id}/versions`** ×5、**`POST /page-versions/{id}/publish`**（均失败）、**`POST /menus`** ×5；**未**触碰 **`component_def`**、**`data_binding`**、**`page_node`** 写接口、**无 `node-tree` PUT** |

### 设计与代码差异（本轮留痕）

| 来源 | 结论 |
|------|------|
| **`前端需求接口文档_v2.0.md` §6.3** | 新建版本请求体示例为 **snake_case**（`version_name`、`source_type`…）。 |
| **curl 实测 `9876`** | **`POST /pages/{pageId}/versions`** **首轮即成功**的请求体为 **camelCase**：`versionName`、`sourceType`、`sourceId`、`remark`；**不必** Fallback snake（未试第二套体亦能落库）。**以实测为准**。 |
| **`src/api/page-version.ts`**（只读复核） | **`createPageVersion`**、**`publishPageVersion`** 在 **`shouldUseRealApi()` 为真时仍走 mock 内存实现**，**未**向 `/pages/{id}/versions`、`/page-versions/{id}/publish` 发 HTTP；与 **`pageVersionApiDrafts`**、“联调应交”不一致。**编辑器若依赖此二函数将无法驱动真实后端**；本轮 **curl** 证实了后端路由存在但与该 TS 实现**未对齐**。 |

### 一、写前 GET（门禁）

```bash
curl.exe --max-time 10 "http://127.0.0.1:9876/pages?current=1&size=100"
curl.exe --max-time 10 "http://127.0.0.1:9876/menus?current=1&size=100"
curl.exe --max-time 10 "http://127.0.0.1:9876/component-defs?current=1&size=100"
curl.exe --max-time 10 "http://127.0.0.1:9876/data-bindings?current=1&size=100"
```

| 检查项 | 结果 |
|--------|------|
| **`pages`** | **`total=0`** |
| **`menus`** | **`total=0`**（载荷为 **`data.items`**） |
| **`component_def`** | **白名单 10 键齐全**；**`test_*` id=12 为 `status:false`**；**`total=16`** |
| **`data_binding`** | **`total=3`** |

### 二、创建 `page`（`POST /pages`，camelCase，与 **`buildPageWriteRequestBodyForBackend`** 一致）

| path | code | 结果 | 返回 **`id`** | **`currentVersionId`（创建响应）** |
|------|------|------|----------------|-------------------------------------|
| `/` | `home` | 成功 | **7** | **`null`** |
| `/news` | `news` | 成功 | **8** | **`null`** |
| `/services` | `services` | 成功 | **9** | **`null`** |
| `/conference` | `conference` | 成功 | **10** | **`null`** |
| `/about/introduction` | `about_introduction` | 成功 | **11** | **`null`** |

说明：**`POST /pages` 成功后未自动回填 `currentVersionId`**。

### 三、`page_version`：创建草稿 + 发布尝试

对每个 **`pageId` 7～11**：**`POST /pages/{pageId}/versions`**，体：

```json
{"versionName":"YAAI skeleton v1","sourceType":"manual","sourceId":null,"remark":"Round4: no node tree yet"}
```

| pageId | 新建 **`page_version.id`** | **`status`**（创建后） |
|--------|---------------------------|------------------------|
| 7 | **308** | `draft` |
| 8 | **309** | `draft` |
| 9 | **310** | `draft` |
| 10 | **311** | `draft` |
| 11 | **312** | `draft` |

**发布**：**`POST /page-versions/{id}/publish`**，body **`{}`**；每个 id **首轮 + 1 次重试**（共 2 次请求）——**全部**

- HTTP **200**，**`success:false`**，**`code: SYSTEM_ERROR`**，**`message`**：「系统繁忙，请稍后再试」。

遵照约束：**不把 `draft` 当作对外 `current_version_id`**；**未**尝试其它臆测路径。

### 四、创建 `menu`（`POST /menus`，camelCase，与 **`buildMenuWriteRequestBodyForBackend`** 一致）

| name | code | sortOrder | **pageId** | 返回 **menu.id** |
|------|------|-----------|------------|------------------|
| 首页 | `nav_home` | 10 | **7** | **9** |
| 学会介绍 | `nav_about` | 20 | **11** | **10** |
| 新闻动态 | `nav_news` | 30 | **8** | **11** |
| 服务矩阵 | `nav_services` | 40 | **9** | **12** |
| 学术会议 | `nav_conference` | 50 | **10** | **13** |

全部 **`urlType: page`**；**`externalUrl: null`**；**未**建分类子菜单。

### 五、写后 GET 验证

| 校验项 | 结果 |
|--------|------|
| **`pages`** | **`total=5`**，`path` 恰为 **`/`、`/news`、`/services`、`/conference`、`/about/introduction`** |
| **`page.currentVersionId`** | **全部为 `null`**（发布阻塞） |
| **`menus`** | **`total=5`**；同上表 **id**；字段符合 **`urlType=page`**、**`pageId`** 与前表 **page.id** 一一对应、**`externalUrl=null`** |
| **`component_def`** | **`total` 仍为 16**（未写） |
| **`data_bindings`** | **`total=3`**（未写） |
| **`GET /pages/{pageId}/versions`** | **每页各 1 条**草稿（**308～312**，`status=draft`） |
| **`GET /page-versions/{308～312}/node-tree`**（**只读**，便于记录空树） | **`data.nodes=[]`**（五版本一致）；**与本轮不写 `page_node`** 的预期一致 |

### 本轮失败项

| 序号 | 操作 | 结果 |
|------|------|------|
| 1 | **`POST /page-versions/308～312/publish`**（各 2 次） | 均 **`SYSTEM_ERROR`**，`success:false` |

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**（**未修改**）、**SQL**、**`component_def`、`data_binding` 写路径**、**`page_node` 写路径**、**`node-tree` PUT**、**`page_template` / `reusable_fragment` 新建**。

### 第四轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-25 | 第四轮：五 **`page`** + 五草稿 **`page_version`** + 五 **`menu`**；**发布接口阻塞**，**`currentVersionId`** 仍为 **`null`** |

---

## 第五轮：currentVersionId 临时绑定（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-25（本地会话，接第四轮） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | 仅 **`PUT /pages/{id}`** 绑定 **`currentVersionId`**；**未**改 **`page_version.status`**、**未**调用 **publish**、**未**建 **page_node** |
| **背景** | 第四轮 **`POST /page-versions/{308～312}/publish`** 均 **`SYSTEM_ERROR`**，**`GET /pages`** 上 **`currentVersionId`** 全 **`null`**，成员 4 无法走 **`page.currentVersionId → node-tree`** |

### 契约 / 代码 vs curl 实测

| 来源 | 结论 |
|------|------|
| **`前端需求接口文档_v2.0.md` §3.2 / §5.4** | **`POST/PUT /pages` 请求体不含 `current_version_id`**，应由 **publish** 维护。 |
| **`src/api/page.ts`**（只读） | **`PageUpdateInput`** 与 **`buildPageWriteRequestBodyForBackend`** **未**映射 **`currentVersionId`**（与 §5.4 一致）。 |
| **curl 实测 `9876`** | **`PUT /pages/{id}`** 在 **camelCase** 全量体中 **附加 `currentVersionId`** 时 **`success:true`** 且 **GET 持久化**——**以 curl 为准**；此为 **联调临时方案**，**非**正式 publish 流程。 |

### 一、写前 GET

| pageId | path | 写前 **`currentVersionId`** | versionId | version **`status`** |
|--------|------|----------------------------|-----------|----------------------|
| **7** | `/` | **`null`** | **308** | **`draft`** |
| **8** | `/news` | **`null`** | **309** | **`draft`** |
| **9** | `/services` | **`null`** | **310** | **`draft`** |
| **10** | `/conference` | **`null`** | **311** | **`draft`** |
| **11** | `/about/introduction` | **`null`** | **312** | **`draft`** |

### 二、单页探测（page 7）

- **`PUT /pages/7`**：基于 **`GET /pages/7`** 字段构造 **camelCase** 体，**仅新增/修改 **`currentVersionId: 308`**。  
- **结果**：**`success:true`**，**`code: SUCCESS`**；**`GET /pages/7`** → **`currentVersionId=308`**。

### 三、批量绑定

| pageId | **`PUT` 目标 `currentVersionId`** | 结果 | 写后 **`GET /pages/{id}`** |
|--------|-----------------------------------|------|---------------------------|
| **7** | **308** | 探测轮已成功 | **308** |
| **8** | **309** | 成功 | **309** |
| **9** | **310** | 成功 | **310** |
| **10** | **311** | 成功 | **311** |
| **11** | **312** | 成功 | **312** |

各 **`PUT`** 首轮即成功，**未触发**重试。

### 四、写后 GET 验证

| 校验项 | 结果 |
|--------|------|
| **`pages.total`** | **5**；**`currentVersionId`** 分别为 **308～312**（与 page **7～11** 一一对应） |
| **`menus.total`** | **5**；**`urlType=page`**、**`pageId`** 仍指向 **7～11**、**`externalUrl=null`** |
| **`GET /page-versions/{308～312}/node-tree`** | 均可 **`success:true`**；**`nodes=[]`**；**`page_version.status` 仍为 `draft`**（**未改**） |
| **`component_def`** | **`total=16`**（未写） |
| **`data_bindings`** | **`total=3`**（未写） |

### 风险与临时性声明

1. **正式流程**仍应 **`POST /page-versions/{id}/publish`** → **`status=published`** + **`page.currentVersionId`** 原子更新；**publish 仍阻塞**（第四轮记录，本轮**未复测** publish）。  
2. 当前 **`currentVersionId`** 指向 **draft** 版本——**未**将 draft **伪装**为 published；成员 4 **仅临时**可拉 **node-tree**（空树可接受）。  
3. **`src/api/page.ts`** 若走 **`updatePage`** 仍**无法**写入 **`currentVersionId`**（适配层未映射）；成员 4 / 运维 **curl** 或后续补 API 适配需知悉。

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**、**SQL**、**`POST` 新建 page/menu/version**、**`page_node` 写**、**`node-tree` PUT**、**`component_def` / `data_binding` 写**、**修改 version status**、**DELETE**。

### 第五轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-25 | 第五轮：**`PUT /pages`** 临时绑定 **`currentVersionId` 308～312**；version 仍为 **draft** |

---

## 第六轮：首页 version 308 node-tree 分步创建（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-25（本地会话，接第五轮） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | 仅 **`POST /page-versions/308/nodes`** ×9（根 + 8 子）；**未** **`node-tree` PUT**、**move**、**copy**、**DELETE** |
| **背景** | 第五轮 **`page 7.currentVersionId=308`** 已绑定；**308** 仍为 **draft**；**publish** 仍阻塞（第四轮） |

### 设计与代码（只读）

| 来源 | 结论 |
|------|------|
| **`component_key白名单.md`** | 本轮 9 个 **`componentKey`** 均在冻结白名单内。 |
| **`src/api/page-node.ts`** | **`buildCreatePageNodeRequestBody`** 使用 **camelCase**（`parentId`、`componentKey`、`propsJson` 等）；与本轮 **curl POST** 体一致。 |
| **契约** | 分步 **`POST /page-versions/{versionId}/nodes`**；**禁止**整树 **PUT**（本轮未调用）。 |

### 一、写前 GET

| 校验项 | 结果 |
|--------|------|
| **`GET /pages/7`** **`path`** | **`/`** |
| **`GET /pages/7`** **`currentVersionId`** | **308** |
| **`GET /page-versions/308/node-tree`** | **`nodes=[]`**（可建树） |
| **`GET /component-defs`** | 白名单 **10 键 `status=true`**；**`total=16`** |

### 二、节点创建结果（`POST /page-versions/308/nodes`，camelCase；JSON 字段为 **字符串** 或 **null**）

**`rootNodeId` = 4124**

| 序号 | componentKey | nodeId | sortOrder | dataBindingId（请求） | 是否被接受 |
|------|--------------|--------|-----------|----------------------|------------|
| 根 | **page_container** | **4124** | **10** | — | — |
| 1 | **hero_banner** | **4125** | **20** | **2** | **是** |
| 2 | **notice_list** | **4126** | **30** | **3** | **是** |
| 3 | **quick_links** | **4127** | **40** | **null** | — |
| 4 | **home_services** | **4128** | **50** | **null** | — |
| 5 | **home_events** | **4129** | **60** | **null** | — |
| 6 | **news_list** | **4130** | **70** | **1** | **是** |
| 7 | **home_shortcuts** | **4131** | **80** | **null** | — |
| 8 | **friend_links** | **4132** | **90** | **null** | — |

- 全部 **9** 次 **POST** **首轮 `success:true`**；**无重试**；**`parentId`** 子节点均持久化为 **4124**（**未**触发「parentId 不生效」停止条件）。
- **每步后 `GET node-tree`**：节点数 **1→9**，**根数始终 1**。

### 三、写后 GET 验证

| 校验项 | 结果 |
|--------|------|
| **`node-tree` 节点数** | **9** |
| **根节点数** | **1**（**4124**，**`componentKey=page_container`**，**`parentId=null`**） |
| **8 子节点 `parentId`** | 均为 **4124** |
| **`componentKey` 空值** | **0** |
| **`sortOrder`** | **10～90**（步长 10） |
| **`slotName`** | 均为 **`main`** |
| **`page 7.currentVersionId`** | 仍为 **308** |
| **`menus`** | **`total=5`** |
| **`component_def`** | **`total=16`** |
| **`data_bindings`** | **`total=3`** |

### 风险声明

- **version 308** 仍为 **`draft`**（**未改 `page_version.status`**）；**`currentVersionId` 绑定 + 手建 node-tree** 均为 **联调临时方案**。
- **正式环境**仍须修复 **`POST /page-versions/{id}/publish`** 后再以 **published** 版本对外。

### 本轮失败项

**无**。

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**、**SQL**、**page / menu / page_version 写**、**component_def / data_binding 写**、**node-tree PUT**、**move / copy / DELETE**、**template / fragment**。

### 第六轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-25 | 第六轮：首页 **308** **9 节点**分步 **POST** 成功（**root 4124**，子 **4125～4132**） |

---

## 第七轮：309～312 最小 rich_text 节点树（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-25（本地会话，接第六轮） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | 仅 **`POST /page-versions/{309\|310\|311\|312}/nodes`** 各 **2** 次（**page_container** + **rich_text**）；**无** **`node-tree` PUT** / **move** / **copy** / **DELETE** |
| **背景** | 第五轮 **`currentVersionId`** 已绑定；各 version 仍为 **draft**；**publish** 仍阻塞（第四轮） |

### 一、写前 GET

| 校验项 | 结果 |
|--------|------|
| **`pages`** | **`total=5`** |
| **`menus`** | **`total=5`** |
| **`component_def`** | **`page_container`**、**`rich_text`** 均 **`status=true`**；**`total=16`** |
| **309～312 `node-tree`** | 均为 **`nodes=[]`**（可建树） |

### 二、节点创建结果（`POST …/nodes`，camelCase；**`dataBindingId=null`**）

| version | 页面 | rootId | richTextId | 结构 | 结果 |
|---------|------|--------|------------|------|------|
| **309** | **`/news`** | **4133** | **4134** | **page_container → rich_text** | **成功**（各 **POST** 首轮 **`success:true`**） |
| **310** | **`/services`** | **4135** | **4136** | 同上 | **成功** |
| **311** | **`/conference`** | **4137** | **4138** | 同上 | **成功** |
| **312** | **`/about/introduction`** | **4139** | **4140** | 同上 | **成功** |

- **`rich_text.parentId`** 均等于对应 **rootId**；**`sortOrder`** 根 **10**、子 **20**；**`slotName=main`**。
- **每版本创建后 `GET node-tree`**：**`nodes=2`**、**根数=1**、**`componentKey` 非空且在白名单**。

### 三、写后总体验证

| 校验项 | 结果 |
|--------|------|
| **308 `node-tree`** | **`nodes=9`**（第六轮，未改） |
| **309～312 `node-tree`** | 各 **`nodes=2`** |
| **5 个 `page.currentVersionId`** | **308～312** 均非 **null** |
| **`menus`** | **`total=5`**，**`pageId`** 仍有效 |
| **`component_def`** | **`total=16`** |
| **`data_bindings`** | **`total=3`** |

### 本轮失败项

**无**。

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**、**SQL**、**page / menu / page_version 写**、**component_def / data_binding 写**、**308 以外 version 的 node 写**、**node-tree PUT**、**move / copy / DELETE**、**template / fragment**。

### 第七轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-25 | 第七轮：**309～312** 各 **2 节点**（**4133～4140**）**rich_text** 最小树成功 |

---

## 第八轮：全链路 GET 冒烟 + 成员4交接文档（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-25（本地会话，接第七轮） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | **仅 GET**（**menus / pages / component-defs / data-bindings / node-tree 308～312**）；**无** 写接口、**无** SQL、**无** 代码改动 |
| **产出** | **[成员4前台联调数据交接清单.md](../02_前台对接/成员4前台联调数据交接清单.md)** |

### 一、全链路 GET 执行清单

| 接口 | 结果 |
|------|------|
| `GET /menus?current=1&size=100` | **`success:true`**，**`total=5`** |
| `GET /pages?current=1&size=100` | **`success:true`**，**`total=5`** |
| `GET /component-defs?current=1&size=100` | **`success:true`**，**`total=16`** |
| `GET /data-bindings?current=1&size=100` | **`success:true`**，**`total=3`** |
| `GET /page-versions/308/node-tree` | **`success:true`**，**`nodes=9`** |
| `GET /page-versions/309/node-tree` | **`success:true`**，**`nodes=2`** |
| `GET /page-versions/310/node-tree` | **`success:true`**，**`nodes=2`** |
| `GET /page-versions/311/node-tree` | **`success:true`**，**`nodes=2`** |
| `GET /page-versions/312/node-tree` | **`success:true`**，**`nodes=2`** |

### 二、验证摘要

**menus**

- **`total=5`**；全部 **`urlType=page`**、**`externalUrl=null`**。
- **`pageId` → path**：9→`/`，10→`/about/introduction`，11→`/news`，12→`/services`，13→`/conference`。
- **`sortOrder`**：10 / 20 / 30 / 40 / 50，顺序合理。

**pages**

- **`total=5`**；**path** 精确为 **`/`、`/news`、`/services`、`/conference`、`/about/introduction`**。
- **`currentVersionId`**：**7→308、8→309、9→310、10→311、11→312**；全部 **`status=true`**。

**node-tree 308**

- **`nodes=9`**；单根 **`page_container`（4124）**；8 子节点 **`parentId=4124`**，无断链。
- **`componentKey`** 均在白名单；**`dataBindingId`**：**4125→2**、**4126→3**、**4130→1**，与 **`data_bindings` id=1/2/3** 对应有效。

**node-tree 309～312**

- 各 **`nodes=2`**；单根 **`page_container`** + 子 **`rich_text`**；**`parentId`** 指向根节点；白名单键合法。

**component_def**

- 白名单 **10 键**均存在且 **`status=true`**。
- **`test_component_1777798334444`（id=12）** **`status=false`**。
- 非白名单保留项：**`container`、`hero_split_container`、`grid_container`、`nav_menu`、`copyright`**。

**data_bindings**

- **`total=3`**；**id=1/2/3** 均存在且 **`status=true`**；首页 node-tree 引用有效；**未修改**。

### 三、本轮失败项

**无**。

### 四、仍存风险

- **`page_version.status`** 仍为 **`draft`**（308～312）。
- **`POST /page-versions/{id}/publish`** 仍 **`SYSTEM_ERROR`**（第四轮起）。
- **`currentVersionId`** 为第五轮 **`PUT /pages`** 临时绑定，非正式 publish 链路。

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**、**SQL**、**一切写接口（POST/PUT/PATCH/DELETE）**。

### 第八轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-25 | 第八轮：全链路 GET 冒烟通过；**成员4交接清单**已生成 |

---

## 第九轮：/services 310、/conference 311 占位节点升级为白名单业务组件（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-30（本地会话） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | 仅 **`PUT /page-nodes/{id}`** ×2，更新 **version 310、311** 的 **rich_text 子节点内容字段**（含 `componentKey`）；**未** 整树 PUT、**未** move/copy/DELETE、**未** 改 page/menu/page_version/component_def/data_binding |
| **背景** | 成员4前台动态渲染第一阶段已落地，`home_services`/`home_events` 已在 `component-map` 映射且不依赖新后端接口；将 310/311 占位 `rich_text` 升级为对应业务组件 |

### 设计与代码复核（只读）

| 来源 | 结论 |
|------|------|
| `component_key白名单.md` | `home_services`、`home_events` 均在冻结白名单内 |
| `src/api/page-node.ts` `buildUpdatePageNodeBodyCamel` | 单节点 PUT 体含 `componentKey`，**不含** `parentId/slotName/sortOrder`（结构变更仅走 move）；支持改 `componentKey` |

### 一、写前 GET

| 校验项 | 结果 |
|--------|------|
| **310 node-tree** | 2 节点：根 `page_container`(4135) + 子 `rich_text`(**4136**，parentId=4135，slot=main，sort=20) |
| **311 node-tree** | 2 节点：根 `page_container`(4137) + 子 `rich_text`(**4138**，parentId=4137，slot=main，sort=20) |
| **component_def** | `home_services`、`home_events` 均 present + `status=true`；total=16 |

### 二、写操作（`PUT /page-nodes/{id}`，camelCase，仅内容字段；JSON 字段为字符串/null）

| 节点 id | version | componentKey 前 → 后 | nodeName | propsJson | 结果 |
|--------:|--------:|----------------------|----------|-----------|------|
| **4136** | 310 `/services` | `rich_text` → **`home_services`** | 服务矩阵 | `{"title":"服务矩阵","subtitle":"提供人工智能学习、科研与实践服务","columns":4,"items":[]}` | **成功**（首轮 `success:true`，`code:SUCCESS`） |
| **4138** | 311 `/conference` | `rich_text` → **`home_events`** | 学术会议 | `{"title":"学术会议","limit":4,"showDate":true}` | **成功**（首轮 `success:true`，`code:SUCCESS`） |

- 请求体均 **未提交** `parentId` / `slotName` / `sortOrder` / `pageVersionId`；`styleJson="{}"`、`layoutJson="{}"`、`eventJson=null`、`visibleRuleJson=null`、`dataBindingId=null`。
- 每次 PUT 后立即 GET 对应 node-tree 验证；无重试触发。

### 三、写后 GET 验证

| 校验项 | 结果 |
|--------|------|
| **310** | 仍 2 节点；根 `page_container`(4135)；子 **4136 `home_services`**，parentId=4135（不断链） |
| **311** | 仍 2 节点；根 `page_container`(4137)；子 **4138 `home_events`**，parentId=4137（不断链） |
| **首页 308** | **未改动**：9 节点，root 4124 + 8 子（hero_banner…friend_links） |
| **pages** | `total=5`（未改） |
| **menus** | `total=5`（未改） |
| **component_def** | `total=16`（未改） |
| **data_bindings** | `total=3`（未改） |

### 本轮失败项

**无**。

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**、**SQL**、**整树 node-tree PUT**、**move / copy / DELETE**、**page / menu / page_version / component_def / data_binding 写**、**308 及 309/312 节点**、**新增接口/页面/菜单**。

### 第九轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-30 | 第九轮：310/311 占位 `rich_text`（4136/4138）经单节点 PUT 升级为 `home_services`/`home_events`，结构不变、首页与其它数据未动 |

---

## 第十轮：309~312 占位升级为可交付展示节点（9876 联调库）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-30（本地会话，接第九轮） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 |
| **本轮范围** | 仅 **`PUT /page-nodes/{id}`** ×4，更新 **version 309~312** 既有子节点内容字段（含 `componentKey`/`dataBindingId`）；**未** 整树 PUT、move、copy、DELETE；**未** 改 page/menu/page_version/component_def/data_binding；**未** 新增 template/fragment |
| **背景** | 成员4 `DynamicPage`/`NodeRenderer`/`component-map` 已落地，`news_list`/`home_services`/`home_events` 可映射；将四页从「第一阶段 rich_text 占位」升级到「可交付展示」 |
| **与第九轮关系** | 310/311 第九轮已升级为 `home_services`/`home_events`；本轮对其**幂等重提**（propsJson 与目标一致），并新增 309→`news_list`、312→`rich_text` 正式文案 |

### 一、写前 GET

| version | path | 节点结构（写前） | 目标 |
|--------:|------|------------------|------|
| 309 | `/news` | 根 `page_container`(4133) + 子 `rich_text`(**4134**) | → `news_list`（dataBindingId=1） |
| 310 | `/services` | 根 `page_container`(4135) + 子 `home_services`(**4136**，第九轮已升级) | 幂等重提 `home_services` |
| 311 | `/conference` | 根 `page_container`(4137) + 子 `home_events`(**4138**，第九轮已升级) | 幂等重提 `home_events` |
| 312 | `/about/introduction` | 根 `page_container`(4139) + 子 `rich_text`(**4140**) | 保持 `rich_text`，更新正式文案 |

- 门禁校验：`news_list`/`home_services`/`home_events`/`rich_text` 均 present + `status=true`（defs total=16）；`data_binding id=1` = `news_category_xhxw`、`status=true`（bindings total=3）。
- 说明：310/311 写前已是目标 key（非占位），结构仍为单根+单子（2 节点），属第九轮结果而非异常，故继续幂等执行。

### 二、写操作（`PUT /page-nodes/{id}`，camelCase，仅内容字段；JSON 字段为字符串/null）

| 节点 id | version | componentKey 前 → 后 | nodeName | dataBindingId | propsJson 关键 | 结果 |
|--------:|--------:|----------------------|----------|:-------------:|----------------|------|
| **4134** | 309 | `rich_text` → **`news_list`** | 新闻动态 | **1** | `title/limit:8/showDate:true` | 成功（`success:true`,`SUCCESS`） |
| **4136** | 310 | `home_services` → `home_services`（幂等） | 服务矩阵 | null | `title/subtitle/columns:4/items:[]` | 成功 |
| **4138** | 311 | `home_events` → `home_events`（幂等） | 学术会议 | null | `title/limit:4/showDate:true` | 成功 |
| **4140** | 312 | `rich_text` → `rich_text`（文案更新） | 学会介绍 | null | `title/content`（正式学会介绍文案） | 成功 |

- 请求体均**未提交** `parentId`/`slotName`/`sortOrder`/`pageVersionId`；`styleJson="{}"`、`layoutJson="{}"`、`eventJson=null`、`visibleRuleJson=null`。
- 顺序执行，任一失败即停止后续；本轮 4 次均首轮 `success:true`，无重试、无失败。

### 三、写后 GET 验证

| 校验项 | 结果 |
|--------|------|
| **309** | 2 节点；根 `page_container`(4133)；子 **4134 `news_list`**，parentId=4133，**dataBindingId=1** ✅ |
| **310** | 2 节点；子 **4136 `home_services`**，parentId=4135 ✅ |
| **311** | 2 节点；子 **4138 `home_events`**，parentId=4137 ✅ |
| **312** | 2 节点；子 **4140 `rich_text`**，parentId=4139，content=正式文案（「云南大学人工智能学会面向全校…」）✅ |
| **首页 308** | **未改动**：9 节点（4124 + 8 子，dataBinding 4125→2/4126→3/4130→1）✅ |
| **pages / menus / component_def / data_bindings** | `5 / 5 / 16 / 3`（均未改）✅ |
| **parentId 断链** | 无 ✅ |
| **componentKey 白名单** | 全部在白名单内（page_container/news_list/home_services/home_events/rich_text）✅ |

### 本轮失败项

**无**。

### 未触碰范围声明

**YAAI**、**`yaai-lowcode/src`**、**SQL**、**整树 node-tree PUT**、**move/copy/DELETE**、**page/menu/page_version/component_def/data_binding 写**、**首页 308 节点**、**新增接口/页面/菜单/template/fragment**。

### 仍存在风险

- **publish**：`POST /page-versions/{id}/publish` 仍 SYSTEM_ERROR（后端未修）；version 仍 `draft`，cvid 经 `PUT /pages` 临时绑定。
- **HomeXXX 数据来源**：`home_services`/`home_events`/`news_list` 在 YAAI 中多由组件内部/自取数据（`HomeServices`/`HomeEvents`/`HomeNews`），`propsJson`（含 `items`）当前仅作可选覆盖，**实际 items 透传为 P2**，不阻塞展示。

### 第十轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-30 | 第十轮：309→`news_list`(dbId=1)、310/311 幂等 `home_services`/`home_events`、312 `rich_text` 正式文案；结构不变、首页与其它表未动 |

---

## 第十一轮：componentKey 语义标准化（首页区块 key 与子页面 key 拆分，9876 联调库 + 成员3 代码）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-05-30（本地会话，接第十轮） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 + `yaai-lowcode/src` 代码 |
| **本轮范围** | **`POST /component-defs`** ×3（新增 key）+ **`PUT /page-nodes/{id}`** ×3（308/310/311 既有子节点）+ 改 `src/constants/yaaichannel-component-keys.ts`、`src/mock/database.ts` + docs；**未** 整树 PUT/move/copy/DELETE，**未** 改 page/menu/page_version/data_binding，**未** 改 309/312 |
| **为什么拆 key** | 此前 `news_list`/`home_services`/`home_events` 同时用于「首页 308 区块」与「子页面 309~311」，同一 key 在首页/子页面需映射不同前台组件，语义含糊。拆为：首页 `home_news`/`home_services`/`home_events`；子页面 `news_list`/`services_page`/`conference_page`。 |

### 一、写前 GET

| 项 | 结果 |
|----|------|
| component_def | total=16；`home_news`/`services_page`/`conference_page` **均不存在** |
| 308 | 9 节点；新闻节点 **4130 = `news_list`**（dbId=1，parent 4124）；另含 home_services(4128)/home_events(4129) 首页区块 |
| 309 | `news_list`(4134) — 本轮不改 |
| 310 | `home_services`(4136) → 目标 `services_page` |
| 311 | `home_events`(4138) → 目标 `conference_page` |
| 312 | `rich_text`(4140) — 本轮不改 |

### 二、`POST /component-defs`（camelCase，JSON 字段字符串/null）

| componentKey | componentName | group/type | isContainer/canBindData | 结果 |
|--------------|---------------|------------|:-----------------------:|------|
| `home_news` | 首页新闻区块 | portal_home/business | false/true | 成功（**id=17**，status=true） |
| `services_page` | 服务矩阵页面 | business/business | false/false | 成功（**id=18**，status=true） |
| `conference_page` | 学术会议页面 | business/business | false/false | 成功（**id=19**，status=true） |

### 三、`PUT /page-nodes/{id}`（仅内容字段，未提交 parentId/slotName/sortOrder/pageVersionId）

| 节点 id | version | componentKey 前 → 后 | dataBindingId | 结果 |
|--------:|--------:|----------------------|:-------------:|------|
| **4130** | 308 | `news_list` → **`home_news`** | 1（保留） | 成功（`success:true`） |
| **4136** | 310 | `home_services` → **`services_page`** | null | 成功 |
| **4138** | 311 | `home_events` → **`conference_page`** | null | 成功 |

- 顺序执行、halt-on-failure；3 次均首轮成功，无重试、无失败。
- **未改** 308 的 home_services(4128)/home_events(4129)（仍为首页区块）；**未改** 309 `news_list`、312 `rich_text`。

### 四、成员3 代码同步

- `src/constants/yaaichannel-component-keys.ts`：`YAAI_OFFICIAL_COMPONENT_KEYS` 新增 `home_news`/`services_page`/`conference_page`（不删旧项，13 项）。
- `src/mock/database.ts`：`componentDefs` 新增 3 条（id 512/513/514，JSON 字段为字符串/null）。
- `npm run type-check`：**通过**（exit 0，无错误）；ReadLints 无告警。

### 五、写后 GET 验证

| 校验项 | 结果 |
|--------|------|
| component_def | total=**19**；`home_news`/`services_page`/`conference_page` present + status=true ✅ |
| 308 | 9 节点；新闻节点 = **`home_news`**；home_services/home_events 仍在；parent 全 4124 不断链 ✅ |
| 309 | `news_list` 不变 ✅ |
| 310 | **`services_page`**（parent 4135）✅ |
| 311 | **`conference_page`**（parent 4137）✅ |
| 312 | `rich_text` 不变 ✅ |
| pages / menus / data_bindings | 5 / 5 / 3（未改）✅ |

### 成员4 需新增 component-map 映射

`home_news` → `HomeNews`、`services_page` → 服务矩阵页组件、`conference_page` → 学术会议页组件。**补齐前**这三类节点在 YAAI 走 `UnknownComponent` 兜底（不白屏）。

### 本轮失败项

**无**。

### 未触碰范围声明

**YAAI**（未改）、**后端代码**、**SQL**、**整树 PUT**、**move/copy/DELETE**、**page/menu/page_version/data_binding 写**、**309/312 节点**、**新增页面/菜单**。

### 第十一轮修订索引

| 日期 | 轮次 |
|------|------|
| 2026-05-30 | 第十一轮：拆首页/子页面 key；新增 component_def 17/18/19；308→home_news、310→services_page、311→conference_page；前端常量与 mock 同步、type-check 通过 |

---

## 第十二轮：废弃 conference_page + /conference 改 rich_text 占位（成员4 确认后，9876 + 成员3 代码）

| 字段 | 内容 |
|------|------|
| **执行时间** | 2026-06-02（本地会话） |
| **执行环境** | `http://127.0.0.1:9876` 联调库 + `yaai-lowcode/src` |
| **本轮范围** | 改 `src/constants` + `src/mock` + **`PUT /component-defs/19`**（停用）+ **`PUT /page-nodes/4138`**（311 子节点改键）；**尝试** `PUT /news-categories/{id}` 改 parentId **被后端 DATA_CONFLICT 阻塞**；**未** 整树 PUT/move/copy/DELETE，**未** 改 page/menu/page_version/news/data_binding |
| **依据** | 成员4 确认：/conference 因后端缺接口暂不启用 → 废弃 conference_page；news_category.parentId→menus.id；news.categoryId→news_category.id 保持 |

### 一、写前 GET（门禁）
- `nav_news` menu.id = **11**（pageId 8 = /news）
- `conference_page` component_def **id=19，status=true**
- version 311 子节点 **id=4138 = conference_page**（parent 4137）
- news-categories 1/2/3 均 **parentId=null**；news.categoryId 分布 1:2/2:2/3:1

### 二、成员3 代码改动
- `src/constants/yaaichannel-component-keys.ts`：白名单移除 `conference_page`（13→**12** 键，保留 home_events 等）；新增 `DEPRECATED_COMPONENT_KEYS=['conference_page']` 与废弃注释。
- `src/mock/database.ts`：conference_page（id 514）→ `status:false` + 废弃备注。
- `npm run type-check`：**通过**（exit 0）；ReadLints 无告警。

### 三、后端写（camelCase，写前/写后 GET）
| 操作 | 结果 |
|------|------|
| `PUT /component-defs/19`（基于 GET 全字段，仅改 `status:false`+remark） | **成功**（status: true→false） |
| `PUT /page-nodes/4138`（componentKey conference_page→**rich_text**，propsJson 占位「学术会议栏目正在建设中…」，未提交 parentId/slotName/sortOrder/pageVersionId） | **成功**（key=rich_text） |

### 四、news-categories.parentId（**被阻塞，未落地**）
- `PUT /news-categories/1`（基于 GET 全字段，仅改 `parentId=11`）→ **`success:false`，`code=DATA_CONFLICT`，「数据冲突」**；**重试 1 次仍失败**，按规则**停止**，未改 2/3、未尝试 PATCH/其它路径。
- **根因**：`news_category.parentId` 在 v6.0 schema 为**指向 `news_category.id` 的自关联外键**（`fk_news_category_parent`）；写入 `menu.id=11` 引用了不存在的 news_category 行 → 外键/数据冲突。**成员4 方案「news_category.parentId → menus.id」与后端现有 schema 冲突**，需后端决策（改外键语义 / 新增字段 / 由前端用其它方式声明归属），非前端可绕过。
- 因此 parentId 仍为 null（未变），news.categoryId 保持不变。

### 五、写后 GET 验证
| 校验项 | 结果 |
|--------|------|
| 308 首页 | 9 节点不变（home_news 等）✅ |
| 309 /news | `news_list` ✅ |
| 310 /services | `services_page` ✅ |
| 311 /conference | **`rich_text`** ✅（不再 conference_page） |
| 312 /about/introduction | `rich_text` ✅ |
| node-tree 中 conference_page | **已无** ✅ |
| component_def conference_page | 仍存在 id19，**status=false** ✅（未 DELETE） |
| news-categories parentId | 仍为 null（**未落地**，DATA_CONFLICT）⚠️ |
| show-list category_id=1/2/3 | 2/2/1 ✅（news.categoryId 不变） |
| pages/menus/data_bindings | 5/5/3（未改）✅ |

### 本轮结果
- **成功**：conference_page 废弃（白名单/mock/后端 def）+ /conference 改 rich_text 占位。
- **失败/阻塞**：news-categories.parentId→menus.id（后端 DATA_CONFLICT，schema 自关联外键冲突）。

### 未触碰范围声明
**YAAI**、**后端代码**、**SQL**、**整树 PUT/move/copy/DELETE**、**page/menu/page_version/news/data_binding 写**、**308/309/310/312 节点**。

### 第十二轮修订索引
| 日期 | 轮次 |
|------|------|
| 2026-06-02 | 第十二轮：conference_page 废弃（白名单/mock/def status=false）、311→rich_text 占位成功；news-categories.parentId→menus.id 被 DATA_CONFLICT 阻塞（schema 自关联外键冲突），停止并登记 |
