# 数据库清洗候选清单

> 本清单为**候选**，**不自动执行删除或 UPDATE**。  
> **数据源**：[清洗前数据快照.md](./清洗前数据快照.md)（汇总自 **2026-05-20** 联调实盘 **GET**，见 [数据库现状只读盘点.md](./数据库现状只读盘点.md)；**后端重启后请重跑 GET 核对**）。  
> **component_key 正式白名单**：[component_key白名单.md](../01_设计基线/component_key白名单.md)。

---

## A. 建议删除（须引用检查 + 人工签字后方可执行）

| 对象类型 | 标识 / 线索 | 依据 | 前置检查 |
|----------|---------------|------|----------|
| **page** | id=**5**，code `integration_test_page_1777794000723`，path `/integration-test-…` | 联调用；`currentVersionId` 常为 **null** | `menu`、`page_version`、`page_node` 是否残留引用 |
| **page** | id=**4**，code **`pg_*`（CamelProbe）** | 探针页 | 同上 |
| **page_version** | 首页 **pageId=1** 下 **304～307**，`draft`，remark **api-test / integration-test** | 联调草稿 | 确认 **`page.current_version_id`** 未指向；无业务依赖预览 |
| **page_node** | 首页 **101** 下并联根 **`4012`～`4020`** 等与主根并列的副本树 | 「首屏分栏-副本」类垃圾并联 | **禁止**先于根树重构盲目删 ——先备份、`GET node-tree` 对比 |
| **component_def** | id=**12**，**`test_component_1777798334444`** | 明示联调 | **已停用（2026-05-21）**：`PUT /component-defs/12` **全量回写**、仅 **`status:false`**；第二轮 `PATCH …/status` 仍 **不可用**（`SYSTEM_ERROR`）。**物理删除**仍须引用检查 + 人工签字，见 **§A** 执行通路 |

**执行通路建议**：优先 **后台 UI / 后台 DELETE API**；**禁止**在未跑 [数据库清洗SQL_待确认.sql](./数据库清洗SQL_待确认.sql) 中注释的引用 SELECT 情况下执行裸 DELETE。

---

## B. 建议停用（保留行，禁用新建与误选）

| 对象 | 标识 | 原因 |
|------|------|------|
| **component_def** | **`container`、`hero_split_container`、`grid_container`、`nav_menu`、`copyright`** | 非冻结白名单；可能被旧模板或存量引用 |
| **menu** | id=**2**（新闻中心）**`status=false`**（若产品决定暂不展示该项） | 若保留停用：须处理子菜单 **4、5** 的 **parentId**，避免「隐形子菜单」策略歧义 |

**建议**：**`component_def.status=false`**（或后台等价开关）先于物理删除。

---

## C. 建议修正（优先后台 UI / PUT 发布 API，慎用 SQL）

| 问题 | 对象 | 建议 | 验证 GET |
|------|------|------|----------|
| **草稿当头版** | `page` id=**2**，`currentVersionId=200` 且 **200=draft** | **发布** 200 或其副本为 **published** 并 **`current_version_id`** 对齐 | `/pages`、`/pages/2/versions`、`/page-versions/200/node-tree` |
| **path 与 YAAI** | `page` id=**3`，`/about` | **二选一**：改 **path→`/about/introduction`**（或产品与前台占位 `/about`） | `/pages`、`/menus` |
| **`urlType` 与外链混用** | `menu` id=**4、5**：`page` + **`externalUrl=/news?category=…`** | 清空误填 **externalUrl**，或 **`url_type=external`**（若产品与契约允许），**新闻条目优先走分类 API** | `/menus` |
| **停用父带子** | **menu 2=false**，子 **4、5** | 迁移 **`parent_id`** 或在 UI 启用父菜单 | `/menus` |
| **首页树不可用** | **page-version 101**：**componentKey 全 null**、**四根并联** | **重建树**：[首页节点树重建方案_待确认.md](./首页节点树重建方案_待确认.md) | `/page-versions/101/node-tree` |
| **新闻双子根** | **page-version 200**：**1024** 与 **4118** 均根 | 合并 **news_list** 到单一 **`page_container`** 下 | `/page-versions/200/node-tree` |
| **关于页形态** | **page-version 300**：轮播为主 | 若为静态栏目：改用 **`rich_text`** 与白名单段落 | `/page-versions/300/node-tree` |
| **~~白名单缺口~~** → **已处理（2026-05-20）** | ~~`component_def` 缺 `home_services` 等四项~~ → 已通过 **`POST /component-defs`** 补齐 **`home_services`、`home_events`、`home_shortcuts`、`friend_links`**（id **13～16**，`portal_home`，`remark` 「YAAI 前台首页白名单组件」）；验证见 **[数据修正执行记录.md](./数据修正执行记录.md)** | ~~后台/UI~~ → **已由 API POST 写入**（仍建议 UI 目视 schema） | `/component-defs` |
| **`test_component`** | **id=12** | **已停用**：2026-05-21 **`PUT /component-defs/12`**（见 **[数据修正执行记录.md](./数据修正执行记录.md)** 第三轮）；`PATCH /component-defs/12/status` **仍失败**，属后端问题但与 **白名单** 对齐流程已解耦。**物理删除**仍见 **§A** | `/component-defs` + `/component-defs/12` + 引用检查 |

详见执行顺序：**[后台API修正操作清单_待确认.md](./后台API修正操作清单_待确认.md)**。

---

## D. 必须人工确认（不允许自动脚本直接删）

| 条目 | 原因 |
|------|------|
| **page id=1/2/3** | **导航与发布主链** |
| **page_version 101 / 300** | **已 published**，任何删节点须备份与复核 |
| **page_version 200** | **与 current 绑定且为 draft**：发布策略须经产品签字 |
| **menu id=1,3**（及待定 4、5） | **顶栏/侧栏语义** |
| **data_binding 1～3** 与 **`source_key`** | 是否与 **CMS / 运行时 API** 仍一致 |
| **page-templates / reusable-fragments** | **`source_id`、`template_id`、`fragment`** 反向引用未知 |
| **多根冗余节点（4012 系）** | 是否绑定 **预览导出/自动化**，删前须有证据 |

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-20 | Mock 增补白名单四条说明（见原记录）；与清洗计划 §12、`src/mock` **无关实盘**时已注明 |
| 2026-05-20 | 按「清洗前数据快照」重排 **A～D**，并挂载操作清单 / 首页树方案链接 |
| 2026-05-20 | **component_def 白名单四轮 POST 补齐**；`test_component_1777798334444` **PATCH 停用失败**记入 **[数据修正执行记录.md](./数据修正执行记录.md)**；§B 中 **`container` / `hero_split_container` / `grid_container` / `nav_menu` / `copyright`** 仍为 **人工确认或暂保留** |
| 2026-05-21 | **`test_component_1777798334444`（id=12）已 `PUT` 停用**；PATCH `…/status` 仍 `SYSTEM_ERROR`，不阻塞后续 **page/page_version/menu** 重建（见 **[数据修正执行记录.md](./数据修正执行记录.md)** 第三轮） |
