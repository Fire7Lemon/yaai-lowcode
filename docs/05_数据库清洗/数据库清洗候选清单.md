# 数据库清洗候选清单

> 本清单为**候选**，**不自动执行删除**。依据 **2026-05-20** `GET http://127.0.0.1:9876/...` 实盘（见 [`数据库现状只读盘点.md`](数据库现状只读盘点.md)）。  
> **component_key 正式白名单**：[component_key白名单.md](../01_设计基线/component_key白名单.md)。

---

## A. 建议删除的测试数据（人工签字后方可执行）

| 对象类型 | 标识 / 线索 | 依据（本轮实测） |
|----------|-------------|------------------|
| **page** | id=**5**，code `integration_test_page_1777794000723`，path `/integration-test-1777794000723` | remark 写明联调、可删除；`currentVersionId` 为空 |
| **page** | id=**4**，code `pg_1777793968739_fwmcy91f`，CamelProbe | 探针页；`currentVersionId` 为空 |
| **page_version** | pageId=1 下 id 304–307（`from-template-api-test` / `it_from_template_*`），均为 **draft** | remark 含 api-test / integration-test |
| **component_def** | id=**12**，`test_component_1777798334444` | remark 明示联调后续删除 |
| **page_node** | 首页版本 **101** 下 **4012、4013、4014、4015、4016、4017、4018、4019、4020** 等「首屏分栏-副本」并联根 | 与主结构 1101 并列多根，疑似复制垃圾（**须先确认无业务依赖**） |

**未建议直接删**

- `data_binding` 1–3：业务命名正常。  
- `page-templates` / `reusable-fragments`：暂保留，删前查 `source_id` / 引用。

---

## B. 建议停用而不是删除的数据

| 对象 | 标识 | 原因 |
|------|------|------|
| **menu** | id=**2**（新闻中心）`status: false` | 已停用；子菜单 4、5 仍挂其下，需决策是否迁移 parent 或停用子菜单 |
| **component_def** | `container`、`hero_split_container`、`grid_container`、`nav_menu`、`copyright` | 非前台白名单但可能被模板/存量节点引用：宜 **status=false** 或文档标记 legacy，而非硬删 |

---

## C. 建议修正的数据（优先后台/API，再考虑 SQL）

| 问题 | 对象 | 建议 |
|------|------|------|
| **当前版本指向草稿** | `page` id=**2**：`currentVersionId=200`，而 version **200** 的 `status=draft` | 发布新版本为 **published** 并更新 `currentVersionId`，或调整业务允许「草稿当头版」的规则（产品与契约） |
| **path 与 YAAI 不符** | `page` id=**3**：`/about` | 改为 **`/about/introduction`**（或补齐前台 `/about` 路由） |
| **菜单语义混乱** | `menu` id=**4**、**5**：`urlType=page` 同时 `externalUrl` 为 **`/news?category=...`** | 改为 YAAI 可用的 **`/news/c/{numericId}`**（需新闻分类 id），或改为 `external`/`纯 query` 并明确契约 |
| **子菜单父级已隐藏** | `menu` id=**4**、**5** 的 **parentId=2**（父已 `status:false`） | 调整父子关系或启用父菜单 |
| **首页节点无 componentKey** | `page-version` **101** 全部节点 **componentKey null** | 在低代码编辑器中补全：**根 `page_container`**、轮播 **`hero_banner`**、通知 **`notice_list`**、新闻 **`news_list`**、快捷 **`quick_links`** 等；删除多余并联根或通过整树重构（仅用 **API camelCase**，禁止错误 snake PUT） |
| **新闻频道双根** | version **200** 节点 **1024** 与 **4118** 均为 `parentId null` | 合并为单根：`news_list` 挂在 `page_container` 之下 |
| **关于页内容与形态** | version **300** 多为 `hero_banner` 嵌套 | 若应为静态「关于」：改用 **`rich_text`** 与白名单段落 |
| **白名单缺口** | `component_def` 缺 **`home_services`**、**home_events`**、**home_shortcuts`**、**friend_links** | 后台新增 definition，占位 props/schema |

---

## D. 必须人工确认的数据（不允许自动脚本删除）

| 条目 | 原因 |
|------|------|
| **page id=1/2/3** | 可能被菜单与环境依赖；删前全链路确认 |
| **menu id=1,3,4,5** | 已关联 page；改 path 影响导航 |
| **page_version 101** | 当前首页 **published**；任何删节点须先备份与双人复核 |
| **page_version 300** | 关于页 **published** |
| **data_binding** 与 **新闻分类 code**（`news_category_xhxw`、`notice_category`） | 与 CMS 真实分类是否一致需业务确认 |
| **多根节点 4012/4015/4018** | 是否仍被某「预览/导出」依赖不明 |
| **page id=4/5** | 若其他自动化任务仍创建同名 path，删前停任务 |

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-20 | 按 9876 实盘重写 A/B/C/D |
