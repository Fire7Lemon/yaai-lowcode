# 数据库直连只读检查报告

> **执行时间**：2026-05-25  
> **对照文档**：[成员4前台联调数据交接清单.md](../02_前台对接/成员4前台联调数据交接清单.md)  
> **配置来源**：`d:\procedure\Java\yaai\yaai-backend\src\main\resources\application.yml`（只读；**本报告不记录密码**）

---

## 1. 检查边界

- 本轮 **只读**：仅执行 `SELECT` / `SHOW` / `DESCRIBE`（通过 Python `mysql.connector` 发起，等价于只读 SQL）。
- **未执行** 任何写 SQL（无 `INSERT` / `UPDATE` / `DELETE` / `TRUNCATE` / `DROP` / `ALTER` / `CREATE` / `REPLACE` / `CALL`）。
- **未执行** 任何 `.sql` 文件。
- **未修改** YAAI、`yaai-lowcode/src`、数据库数据。
- **未清空** 任何表。
- 发现异常 **只记录，不修复**。

---

## 2. 数据库连接信息

| 项 | 值 |
|----|-----|
| **数据库名** | `yaai_db` |
| **当前用户** | `fail***@%`（打码） |
| **MySQL 版本** | `8.0.41-0ubuntu0.22.04.1` |
| **数据源主机** | `110.41.***.***:3306`（打码；来自后端 `application.yml`） |
| **后端服务端口** | `9876`（与历史 curl 联调一致） |
| **密码** | **未记录** |

**连通性**：`SELECT DATABASE()` / `SELECT CURRENT_USER()` / `SELECT VERSION()` 均成功。

---

## 3. 表结构确认

`SHOW TABLES` 共 **34** 张表。低代码相关表 **均存在**，表名与契约一致（snake_case）：

| 表名 | 是否存在 | 主要字段（`SHOW COLUMNS` 摘要） |
|------|:--------:|--------------------------------|
| `page` | ✅ | `id, name, code, path, page_type, current_version_id, status, ...` |
| `page_version` | ✅ | `id, page_id, version_no, version_name, status, source_type, ...` |
| `page_node` | ✅ | `id, page_version_id, parent_id, node_type, component_key, props_json, data_binding_id, ...` |
| `menu` | ✅ | `id, parent_id, name, code, url_type, page_id, external_url, sort_order, status, ...` |
| `component_def` | ✅ | `id, component_key, component_name, component_group, status, ...` |
| `data_binding` | ✅ | `id, name, binding_type, source_key, status, ...` |
| `page_template` | ✅ | `id, name, code, scene_type, status, ...` |
| `reusable_fragment` | ✅ | `id, name, code, fragment_type, status, ...` |

**表名差异**：无；实际表名与检查清单一致。

---

## 4. 表数量统计

| 表 | COUNT(*) | 交接清单预期 | 是否一致 |
|----|:--------:|-------------|:--------:|
| `page` | **5** | 5 | ✅ |
| `menu` | **5** | 5 | ✅ |
| `page_version` | **5** | 5（308～312） | ✅ |
| `page_node` | **17** | 9 + 4×2 = 17 | ✅ |
| `component_def` | **16** | 16 | ✅ |
| `data_binding` | **3** | 3 | ✅ |
| `page_template` | **0** | 未要求有数据 | ✅ |
| `reusable_fragment` | **0** | 未要求有数据 | ✅ |

---

## 5. page 检查结果

| id | name | code | path | page_type | current_version_id | status | 是否符合预期 |
|----|------|------|------|-----------|-------------------:|:------:|:------------:|
| 7 | 首页 | `home` | `/` | `portal` | **308** | **1** | ✅ |
| 8 | 新闻动态 | `news` | `/news` | `portal` | **309** | **1** | ✅ |
| 9 | 服务矩阵 | `services` | `/services` | `portal` | **310** | **1** | ✅ |
| 10 | 学术会议 | `conference` | `/conference` | `portal` | **311** | **1** | ✅ |
| 11 | 学会介绍 | `about_introduction` | `/about/introduction` | `portal` | **312** | **1** | ✅ |

**摘要**：

- 共 **5** 条；`path` 与交接清单 §3 完全一致。
- `current_version_id` 分别为 **308～312**，与交接清单一致。
- `status=1`（DB 为 tinyint；API 层映射为 `true`）。
- **观察**：5 条 `created_at` / `updated_at` 均为 **NULL**（不影响联调，仅记录）。

---

## 6. menu 检查结果

| id | name | code | url_type | page_id | external_url | sort_order | 是否符合预期 |
|----|------|------|----------|--------:|--------------|----------:|:------------:|
| 9 | 首页 | `nav_home` | `page` | 7 | **NULL** | **10** | ✅ |
| 10 | 学会介绍 | `nav_about` | `page` | 11 | **NULL** | **20** | ✅ |
| 11 | 新闻动态 | `nav_news` | `page` | 8 | **NULL** | **30** | ✅ |
| 12 | 服务矩阵 | `nav_services` | `page` | 9 | **NULL** | **40** | ✅ |
| 13 | 学术会议 | `nav_conference` | `page` | 10 | **NULL** | **50** | ✅ |

**摘要**：

- 共 **5** 条；全部 `url_type='page'`、`external_url IS NULL`。
- `page_id` 均指向有效 `page`（7～11）。
- `sort_order` 为 **10 / 20 / 30 / 40 / 50**，与交接清单 §4 一致。

---

## 7. page_version 检查结果

| id | page_id | version_no | version_name | status | source_type | 是否符合预期 |
|----|--------:|-----------:|--------------|--------|-------------|:------------:|
| 308 | 7 | 1 | YAAI skeleton v1 | **`draft`** | `manual` | ✅ |
| 309 | 8 | 1 | YAAI skeleton v1 | **`draft`** | `manual` | ✅ |
| 310 | 9 | 1 | YAAI skeleton v1 | **`draft`** | `manual` | ✅ |
| 311 | 10 | 1 | YAAI skeleton v1 | **`draft`** | `manual` | ✅ |
| 312 | 11 | 1 | YAAI skeleton v1 | **`draft`** | `manual` | ✅ |

**摘要**：

- **308～312** 均存在；`page_id` 对应 **7～11**。
- 全部 **`status='draft'`** — 与交接清单 §1、§8 一致；原因为 **`POST …/publish` 仍 SYSTEM_ERROR**（第四轮起），**本轮只记录，不修改**。

---

## 8. page_node 检查结果

### 8.1 按 versionId 汇总

| versionId | node 数量 | 根节点数量 | componentKey 列表 | parentId 断链 | 是否符合预期 |
|----------:|:---------:|:----------:|-------------------|:-------------:|:------------:|
| **308** | **9** | **1**（4124） | `page_container`, `hero_banner`, `notice_list`, `quick_links`, `home_services`, `home_events`, `news_list`, `home_shortcuts`, `friend_links` | **否** | ✅ |
| **309** | **2** | **1**（4133） | `page_container`, `rich_text` | **否** | ✅ |
| **310** | **2** | **1**（4135） | `page_container`, `rich_text` | **否** | ✅ |
| **311** | **2** | **1**（4137） | `page_container`, `rich_text` | **否** | ✅ |
| **312** | **2** | **1**（4139） | `page_container`, `rich_text` | **否** | ✅ |

### 8.2 308 首页节点明细（与交接清单 §5 对照）

| nodeId | component_key | parent_id | sort_order | data_binding_id |
|-------:|---------------|----------:|-----------:|----------------:|
| 4124 | `page_container` | NULL | 10 | NULL |
| 4125 | `hero_banner` | 4124 | 20 | **2** |
| 4126 | `notice_list` | 4124 | 30 | **3** |
| 4127 | `quick_links` | 4124 | 40 | NULL |
| 4128 | `home_services` | 4124 | 50 | NULL |
| 4129 | `home_events` | 4124 | 60 | NULL |
| 4130 | `news_list` | 4124 | 70 | **1** |
| 4131 | `home_shortcuts` | 4124 | 80 | NULL |
| 4132 | `friend_links` | 4124 | 90 | NULL |

### 8.3 309～312 最小树（与交接清单 §6 对照）

| version | rootId | richTextId | rich_text.parent_id |
|--------:|-------:|-----------:|--------------------:|
| 309 | 4133 | 4134 | 4133 |
| 310 | 4135 | 4136 | 4135 |
| 311 | 4137 | 4138 | 4137 |
| 312 | 4139 | 4140 | 4139 |

**白名单校验**：上述全部 `component_key` 均在 [component_key白名单.md](../01_设计基线/component_key白名单.md) 10 键内。

---

## 9. component_def 检查结果

### 9.1 白名单 10 键

| component_key | id | status | 是否符合预期 |
|---------------|---:|:------:|:------------:|
| `page_container` | 1 | **1** | ✅ |
| `hero_banner` | 5 | **1** | ✅ |
| `notice_list` | 6 | **1** | ✅ |
| `quick_links` | 8 | **1** | ✅ |
| `home_services` | 13 | **1** | ✅ |
| `home_events` | 14 | **1** | ✅ |
| `news_list` | 7 | **1** | ✅ |
| `home_shortcuts` | 15 | **1** | ✅ |
| `friend_links` | 16 | **1** | ✅ |
| `rich_text` | 9 | **1** | ✅ |

**结论**：白名单 **10 键齐全**，且 **`status=1`（启用）**。

### 9.2 测试组件

| component_key | id | status | 是否符合预期 |
|---------------|---:|:------:|:------------:|
| `test_component_1777798334444` | 12 | **0**（禁用） | ✅ |

### 9.3 非白名单保留项（只记录，不删除）

| component_key | id | status |
|---------------|---:|:------:|
| `container` | 2 | 1 |
| `hero_split_container` | 3 | 1 |
| `grid_container` | 4 | 1 |
| `nav_menu` | 10 | 1 |
| `copyright` | 11 | 1 |

---

## 10. data_binding 检查结果

| id | name | binding_type | source_key | status | 首页节点引用 |
|----|------|--------------|------------|:------:|--------------|
| **1** | 学会新闻列表 | `article_category` | **`news_category_xhxw`** | 1 | 节点 **4130** `news_list`（`data_binding_id=1`） |
| **2** | 首页轮播图 | `banner_group` | **`home_banner`** | 1 | 节点 **4125** `hero_banner`（`data_binding_id=2`） |
| **3** | 通知公告列表 | `article_category` | **`notice_category`** | 1 | 节点 **4126** `notice_list`（`data_binding_id=3`） |

**结论**：**id=1/2/3 均存在**；`source_key` 与交接清单 §5、`dataBindingId` 引用 **一致**。

---

## 11. 与成员4交接数据是否一致

对照 [成员4前台联调数据交接清单.md](../02_前台对接/成员4前台联调数据交接清单.md)：

| 维度 | 交接清单 | 数据库实测 | 是否一致 |
|------|---------|-----------|:--------:|
| **menus** | 5 条；`urlType=page`；sortOrder 10～50；pageId 7/8/9/10/11 | 一致 | ✅ |
| **pages** | 5 条；path 五路径；currentVersionId 308～312 | 一致 | ✅ |
| **node-tree 308** | 9 节点；单根 4124；8 子节点；dataBinding 1/2/3 | 一致 | ✅ |
| **node-tree 309～312** | 各 2 节点；4133～4140；page_container + rich_text | 一致 | ✅ |
| **component_def** | 16 条；白名单 10 键启用；test id=12 禁用 | 一致 | ✅ |
| **data_bindings** | 3 条；source_key 与首页引用匹配 | 一致 | ✅ |

**总判断**：**数据库低代码核心数据与成员4交接清单一致**。

---

## 12. 发现的问题

> **只记录，不修复。**

| # | 类型 | 说明 | 是否阻塞联调 |
|:-:|------|------|:------------:|
| 1 | **已知风险（非数据不一致）** | `page_version.status` 全为 **`draft`**；`POST …/publish` 仍失败 | 否（联调约定以 `current_version_id` 为准） |
| 2 | **观察项** | `page.created_at` / `page.updated_at` 均为 **NULL** | 否 |
| 3 | **观察项** | `page_template` / `reusable_fragment` 表 **COUNT=0** | 否（本轮未使用模板/片段） |
| 4 | **观察项** | 非白名单 `component_def` 5 条仍保留且启用 | 否（历史定义；新节点不使用即可） |
| 5 | **配置项（非 DB 数据）** | 后端 `application.yml` 含敏感信息（Redis/邮件等）；**本报告未记录密码** | — |

**未发现** 与交接清单不一致的 page / menu / page_node / component_def / data_binding **数据差异**。

---

## 13. 下一步建议

**交给成员4继续前台动态渲染** — 数据库实测与 [成员4前台联调数据交接清单.md](../02_前台对接/成员4前台联调数据交接清单.md) **一致**；可按 [成员4向低代码适配实现指南.md](../02_前台对接/成员4向低代码适配实现指南.md) 开始 P0/P1 实现（先解 `/news`、`/about/introduction` 路由阻塞，再补 `NodeRenderer` 运行时）。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-25 | 首版：直连 `yaai_db` 只读检查；与成员4交接清单逐项对照，结论一致 |
