# 联调验收报告（浏览器与环境配置）

> **生成时间**：2026-05-30
> **本轮目标**：解决上一轮只读报告 P0（缺 `.env` 导致前端请求未走 `/api` 代理到 9876），完成环境配置 + 代理验证 + 浏览器联调验收。
> **本轮改动范围**：仅新增两个本地环境文件 `YAAI/.env.local`、`yaai-lowcode/.env.local` + 本报告；**未改任何业务源码 / `YAAI/src` / `yaai-lowcode/src` / 后端 / vite 配置 / `.env.example`**；**未执行 SQL、未调用写接口、未格式化、未 `npm install`**。
> **关联**：[联调验收报告_只读.md](./联调验收报告_只读.md)、[成员3实现进度检查报告.md](./成员3实现进度检查报告.md)、[后端待修复清单.md](./04_后端修复/后端待修复清单.md)。

---

## 1. 本轮权限边界

| 约束 | 执行结果 |
|------|----------|
| 不改业务源码 / `YAAI/src` / `yaai-lowcode/src` / 后端 | ✅ |
| 不执行 SQL、不调用 POST/PUT/PATCH/DELETE | ✅ 仅 GET |
| 不格式化、不 `eslint --fix` / `prettier --write` | ✅ |
| 仅允许新增/改 `*.env.local`（本地联调，无密码/无 DB 连接串） | ✅ 仅写 `VITE_*` |
| 不改 `.env.example` | ✅ |
| 仅允许写 `联调验收报告_浏览器.md` | ✅ |

---

## 2. 上一轮只读报告摘要

- **上一轮 P0**：两端缺实际 `.env`，YAAI 应用 `axios.baseURL = import.meta.env.VITE_API_BASE_URL` 为 `undefined` → 请求打到裸路径 `/menus`（实测返回 SPA HTML），未命中 `/api` 代理；yaai-lowcode 因 `VITE_USE_REAL_API` 未设 → 默认 mock 模式，不读后端。
- **本轮需验证**：创建 `.env.local` 并重启后，两端是否经 `/api` 代理拿到真实后端 JSON；后台管理端是否切到真实数据；前台是否能跑 DynamicPage 链路。
- **联调临时约定**：`page.currentVersionId` 由 `PUT /pages` 临时绑定；全版本 `draft`；**不依赖 `version.status==='published'`**，以「cvid 能拉到 node-tree」为准；node-tree 一律 camelCase。

---

## 3. env 配置检查与处理

### 3.1 处理前（仅存在性，无敏感值）

| 文件 | 处理前 |
|------|--------|
| `YAAI/.env.local` | 不存在 |
| `YAAI/.env` | 不存在 |
| `YAAI/.env.example` | 存在（含 `VITE_API_BASE_URL`） |
| `yaai-lowcode/.env.local` | 不存在 |
| `yaai-lowcode/.env` | 不存在 |
| `yaai-lowcode/.env.example` | 不存在 |

> 两个 `.env.local` 均不存在 → 本轮**新建**（无覆盖既有文件）。

### 3.2 本轮新增（仅本地联调变量，无密码/无 DB 串）

- `YAAI/.env.local`：
  - `VITE_API_BASE_URL=/api`
- `yaai-lowcode/.env.local`：
  - `VITE_USE_REAL_API=true`
  - `VITE_API_BASE_URL=/api`

> 依据：`YAAI/src/utils/request.ts` 用 `VITE_API_BASE_URL` 作 axios baseURL；`yaai-lowcode/src/api/http-client.ts` 的 `shouldUseRealApi()` 读 `VITE_USE_REAL_API==='true'`、`getApiBaseUrl()` 读 `VITE_API_BASE_URL`。Vite 在 dev 模式会加载 `.env.local`（优先级高于 `.env`），重启后生效。

---

## 4. Vite proxy 检查

| 项目 | `/api` 代理目标 | rewrite | 结论 |
|------|-----------------|---------|------|
| YAAI（`vite.config.ts`） | `http://localhost:9876` | `path.replace(/^\/api/, '')` 去掉 `/api` | ✅ 正确指向 9876 |
| yaai-lowcode（`vite.config.js`） | `http://localhost:9876` | 同上 | ✅ 正确指向 9876 |

均为 `localhost:9876`（与后端 `127.0.0.1:9876` 等价）。未改任何配置。

---

## 5. 后端 9876 GET 检查

| 接口 | 结果 |
|------|------|
| `GET /pages?current=1&size=100` | success=true，total=**5** |
| `GET /menus?current=1&size=100` | success=true，total=**5** |
| `GET /page-versions/308/node-tree` | success=true，nodes=**9** |

后端在线（首探 HTTP 200 application/json），数据与交接清单一致。

---

## 6. 前端 dev 服务启动结果

| 项 | 结果 |
|----|------|
| `node_modules` | 两端均存在（未 `npm install`） |
| 旧 dev 进程 | `.env.local` 创建前已在运行 → 已 `taskkill /T /F` 停止旧进程树后**重启**（确保加载 `.env.local`） |
| YAAI 重启 | ✅ `http://localhost:5173/`（VITE v8，ready） |
| yaai-lowcode 重启 | ✅ `http://localhost:5174/`（5173 被占用自动改 5174，VITE v7，ready） |
| 启动错误 | 无 error/EADDRINUSE |
| YAAI 启动期 Console（观察） | 出现 Vue Router `No match found`：`/services/consulting`、`/services/training`、`/services/project`、`/services/transformation`、`/news/1`、`/news/2`、`/member/join`（**warn，非 error**，见 §11 P2） |

---

## 7. 代理 GET 验证结果

> dev 服务重启后，经各自 `/api` 代理实测（GET）。

### YAAI :5173 `/api`

| 接口 | content-type | 结果 |
|------|--------------|------|
| `/api/menus?...` | application/json | success=true，total=**5** |
| `/api/pages?...` | application/json | success=true，total=**5** |
| `/api/page-versions/308/node-tree` | application/json | success=true，nodes=**9** |
| `/api/page-versions/312/node-tree` | application/json | success=true，nodes=**2** |

### yaai-lowcode :5174 `/api`

| 接口 | content-type | 结果 |
|------|--------------|------|
| `/api/pages?...` | application/json | success=true，total=**5** |
| `/api/menus?...` | application/json | success=true，total=**5** |
| `/api/page-versions/308/node-tree` | application/json | success=true，nodes=**9** |
| `/api/component-defs?...` | application/json | success=true，total=**16** |

**全部返回 JSON（非 HTML）、`success=true`、计数正确** → **上一轮 P0 代理/环境问题已解除**。

> 对比上一轮：彼时 YAAI 裸路径 `/menus` 返回 text/html；本轮 `/api/*` 全部为 application/json。`.env.local` 写入 `VITE_API_BASE_URL=/api` 后，应用层 axios 将以 `/api` 前缀发起请求并命中代理。

---

## 8. YAAI 浏览器验收结果

> **未进行可视化浏览器验证**：本环境无浏览器自动化工具，未实际打开页面/截图/读 DevTools。以下基于「`.env.local` 已配 + dev 重启 + `/api` 代理实测 JSON + 代码扫描」推断，**不声称已可视化验证**。

| 验收项 | 推断结论 | 依据 |
|--------|----------|------|
| `/` 首页 | 应可渲染（→DynamicPage→`HomeDynamicContent` 渲染 308 九节点） | 路由注册 `SitePage_7`；`/api/.../308/node-tree` nodes=9 |
| `/about/introduction` 进 DynamicPage | 应可进入 | `shouldRegisterDynamicRoute` 放开 portal；注册 `SitePage_11`；312 nodes=2 |
| `/about/introduction` 显示 rich_text | 应可显示 | `RichTextBlock` + 312 含 rich_text 节点 |
| `/news` 进 DynamicPage / 显示 rich_text | 应可 | 注册 `SitePage_8`；309 nodes=2（rich_text） |
| `/services`、`/conference` 可访问 | 应可（同走 DynamicPage，310/311 rich_text） | 注册 `SitePage_9/10` |
| 白屏 | 预期否 | NodeRenderer `onErrorCaptured` + safeJsonParse 容错 |
| 未知 componentKey 兜底 | 具备 | `getComponentByKey`→`UnknownComponent` |
| 是否依赖 `version.status==='published'` | 否 | DynamicPage 仅看 `currentVersionId` |

> **需用户实跑确认**：浏览器打开上述 5 路径，看 Network 出现 `/api/menus`、`/api/pages`、`/api/page-versions/{308,309,312...}/node-tree`（均 200 JSON），且页面非白屏、Console 无报错。

---

## 9. yaai-lowcode 浏览器验收结果

> 同上，**未进行可视化浏览器验证**。`.env.local` 已设 `VITE_USE_REAL_API=true` → 应用进入真实模式（不再走 mock）。

| 验收项 | 推断结论 | 依据 |
|--------|----------|------|
| 页面管理显示真实 **5** 页（非 mock 2 条） | 应是 | 真实模式 + `/api/pages` total=5 |
| 菜单管理显示真实 **5** 菜单 | 应是 | `/api/menus` total=5 |
| 组件定义 total=**16** | 应是 | `/api/component-defs` total=16 |
| 首页版本 308 编辑器显示 **9** 节点 | 应是 | `/api/page-versions/308/node-tree` nodes=9 |
| 未点击保存/删除/移动/新建 | ✅ 未触发任何写操作 | — |

> **需用户实跑确认**：浏览器打开 `:5174`，进入页面/菜单/组件定义/首页 308 编辑器核对计数。

---

## 10. Network / Console 观察

- **Network（代理层实测）**：`/api/menus`、`/api/pages`、`/api/page-versions/308|312/node-tree`、`/api/component-defs` 经两端 dev 代理均 200 + application/json + `success=true`。
- **Console（dev server 端日志观察）**：YAAI 启动期出现 Vue Router `No match found`（`/services/consulting`、`/services/training`、`/services/project`、`/services/transformation`、`/news/1`、`/news/2`、`/member/join`）——为 **warn**，源于深层子路径未注册路由（顶层 5 页正常）；非本轮联调阻塞项。
- **浏览器内 Console**：未观测（无浏览器工具）。

---

## 11. 问题分级

### P0（阻塞浏览器联调）

- **无**。环境 P0 已解除：`.env.local` 已配、dev 已重启、`/api` 代理实测返回真实 JSON。后端 + 成员3数据 + 成员4运行时代码 + 代理链路均就绪。

### P1（联调可继续，但需修）

- **P1-A｜`yaai-lowcode/src/api/page-version.ts` 版本管理真实分支缺失（假成功风险）**：真实模式下 `createPageVersion/deletePageVersion/publishPageVersion/clonePageVersion/lock/unlock` 仍走内存 mock。归属**成员3**。本轮真实模式开启后，**切勿在管理端点击版本"新建/删除/发布/复制/锁定"**（会假成功，不落库）。下一轮补真实分支。
- **P1-B｜后端 `POST /page-versions/{id}/publish` SYSTEM_ERROR / 全版本 draft / cvid 经 PUT 临时绑定**：归属**后端**；联调期以 cvid + node-tree 兜住，不阻塞；正式上线前必修。

### P2（优化）

- **P2-1｜YAAI 深层子路径无路由**：`/services/*`、`/news/{id}`、`/member/join` 等 Vue Router `No match`（warn）。归属**成员4**前台导航/路由，超出本轮低代码联调范围。
- **P2-2｜docs 与实测不同步**：02_前台对接 旧文档仍称成员4 渲染层"缺失"，实测已落地。归属成员3/4 协同（仅文档）。
- **P2-3｜列表分页字段不统一**（pages=`records`、menus=`items`）：后端，已前端双兼容，不阻塞。
- **P2-4｜309~312 rich_text 占位文案、`page.created_at/updated_at` 为 NULL**：后续打磨。
- **P2-5｜`.env.local` 未纳入版本约定**：建议补 `yaai-lowcode/.env.example`（当前缺）并在 README 注明真实模式开关（归属成员3，非阻塞）。

---

## 12. 当前结论

- **是否可继续交给成员4浏览器联调**：**是**。环境 P0 已解除，`/api` 代理实测返回真实数据，前台运行时代码已就绪。剩余仅"人工浏览器实跑确认"（本环境无浏览器工具，未可视化验证）。
- **是否需要成员3先修**：**否**（P1-A 不阻塞只读联调，下一轮处理；联调期避免点击版本写操作即可）。
- **是否需要后端先修**：联调期**否**；正式上线前需修 `publish`。

---

## 13. 下一轮建议

> 唯一建议：**让成员4（及验收人）在浏览器实跑联调验收**。

具体：保持后端 9876 + 两端 dev（YAAI `:5173`、lowcode `:5174`）运行，浏览器依次打开 YAAI `/`、`/about/introduction`、`/news`、`/services`、`/conference` 与 lowcode 页面/菜单/组件定义/308 编辑器，核对：页面非白屏、Network `/api/*` 全 200 JSON、Console 无 error、rich_text 正常、管理端显示真实 5 页/5 菜单/16 组件/308 九节点。**联调期不要在管理端点击版本新建/删除/发布（P1-A 假成功）。**

---

## 14. 增量（2026-05-30 第九轮）：310/311 占位节点升级

- 将 `/services`(310)、`/conference`(311) 的子节点 `rich_text` 经**单节点 `PUT /page-nodes/{id}`** 升级为白名单业务组件：
  - 节点 **4136**：`rich_text` → **`home_services`**（propsJson：title/subtitle/columns/items[]）
  - 节点 **4138**：`rich_text` → **`home_events`**（propsJson：title/limit/showDate）
- 写后 GET 验证：310/311 仍各 2 节点、根 `page_container` 不变、parentId 不断链；首页 308（9 节点）、pages=5、menus=5、component_def=16、data_bindings=3 **均未改动**。
- 未执行整树 PUT / move / copy / DELETE；未改 page/menu/page_version/component_def/data_binding。
- 影响：`/services` 现走 `home_services`（YAAI `HomeServices.vue`）、`/conference` 走 `home_events`（YAAI `HomeEvents.vue`），不再是 rich_text 占位；二者**不依赖新后端接口**。详见 [数据修正执行记录.md 第九轮](./05_数据库清洗/数据修正执行记录.md)。

---

## 15. 增量（2026-05-30 第十轮）：309~312 升级为可交付展示节点

- 通过**单节点 `PUT /page-nodes/{id}`** 将四页子节点升级（结构不变，仍单根 `page_container` + 单子）：
  - 节点 **4134**（309 `/news`）：`rich_text` → **`news_list`**，`dataBindingId=1`（`news_category_xhxw`）
  - 节点 **4136**（310 `/services`）：**`home_services`**（幂等重提）
  - 节点 **4138**（311 `/conference`）：**`home_events`**（幂等重提）
  - 节点 **4140**（312 `/about/introduction`）：保持 **`rich_text`**，`content` 更新为正式学会介绍文案
- 写后 GET 验证：309~312 各仍 2 节点、根 `page_container` 不变、`parentId` 不断链、componentKey 均在白名单；首页 308（9 节点）、pages=5、menus=5、component_def=16、data_bindings=3 **均未改动**。
- 未执行整树 PUT / move / copy / DELETE；未改 page/menu/page_version/component_def/data_binding；未新增 template/fragment。
- 影响：四个栏目页均从「第一阶段 rich_text 占位」进入「可交付展示」态。详见 [数据修正执行记录.md 第十轮](./05_数据库清洗/数据修正执行记录.md)。
- 仍存风险：publish 未修、version 仍 draft、`HomeXXX`/`HomeNews` 数据多由前台组件内部/自取（`propsJson.items` 透传为 P2）。

---

## 16. 增量（2026-05-30 第十一轮）：componentKey 语义标准化（首页/子页面 key 拆分）

- **后端**：`POST /component-defs` 新增 `home_news`(id17)、`services_page`(id18)、`conference_page`(id19)，全 status=true（component_def total 16→**19**）。
- **node-tree 单节点 PUT**（结构不变、parentId 不断链）：
  - 308 新闻节点 **4130**：`news_list` → **`home_news`**（dataBindingId=1 保留）；首页其余 8 节点不变（含 home_services/home_events）
  - 310 **4136**：`home_services` → **`services_page`**
  - 311 **4138**：`home_events` → **`conference_page`**
  - 309（`news_list`）、312（`rich_text`）**未改**
- **成员3 代码**：`src/constants/yaaichannel-component-keys.ts`（+3 键，共 13）、`src/mock/database.ts`（+3 component_def，id 512/513/514）；`npm run type-check` **通过**，无 lint 告警。
- 写后验证：defs=19、308 新闻→home_news、310→services_page、311→conference_page、309/312 不变、pages=5/menus=5/data_bindings=3，全部 parentId 不断链。
- **成员4 待补 `component-map` 映射**：`home_news`→HomeNews、`services_page`→服务矩阵页组件、`conference_page`→学术会议页组件；补齐前这三类节点走 `UnknownComponent` 兜底（不白屏）。详见 [数据修正执行记录 第十一轮](./05_数据库清洗/数据修正执行记录.md) 与 [component_key白名单 §1.5](./01_设计基线/component_key白名单.md)。
- 未改 YAAI、未改后端代码、未执行 SQL、未整树 PUT/DELETE/move/copy。

---

## 附：本轮运行中的本地服务（用户可自行停止）

- YAAI dev：`http://localhost:5173/`
- yaai-lowcode dev：`http://localhost:5174/`

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-30 | 首版：新增两端 `.env.local`，重启 dev，`/api` 代理实测返回真实 JSON，上一轮环境 P0 已解除；浏览器可视化验收未执行（无浏览器工具），结论基于 curl/proxy/代码推断 |
